/node_modules
/dist
/coverage
*.log
.env
.env.local

# Per-environment config lives in /config (secrets). Only the committed
# example.* templates are tracked; the real .env.* files are ignored.
/config/.env
/config/.env.*

.DS_Store
