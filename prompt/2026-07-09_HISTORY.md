# 2026-07-09 — History

## Prompt
> Let a test case be opened and edited in a full modal like [screenshot] — use the UX/UI design to build it.

## Work done
- **New `CaseEditDialog`** (`src/features/reports/components/CaseEditDialog.tsx`) — a wide three-column "Edit test case" modal built on Radix Dialog + our design-system primitives, matching the reference:
  - **Header**: a brand-tinted rounded icon badge (`bg-primary/10`) + "Edit test case" title + "Make changes, then save to apply" subtitle + × close.
  - **Column 1 (details)**: Name (edits `area`, with the `shortId` shown as a mono chip on the label), Owner (`Select` over the project users + Unassigned), Precondition, Expected result, Actual result.
  - **Column 2 (test steps)**: numbered rows (badge + `Textarea` + × remove) and a full-width **+ Add step**.
  - **Column 3 (note)**: a tall `Textarea` filling the column.
  - **Footer**: Cancel (ghost) + Save (brand primary — per the app's "buttons use branding color" rule, in place of the reference's black button).
  - Every section header uses a purple lucide icon + uppercase label. Edits a local **draft**; Save commits, Cancel/×/overlay/Esc discards.
- **Wired into the Test Case Summary** (`ReportSections` `Testing`): the case **ID is now a clickable `.case-open-btn`** (mono, hovers to brand) that opens the modal for writers; `onSave` writes the full draft back via `setCase(id, draft)` → `onChange` → `useReplaceSections`. Read-only viewers still see the plain id.

Verified with `tsc --noEmit`, `vite build`, and a headless-Chrome screenshot of the compiled dialog — three columns, icon labels, steps, note, and footer matching the design.

---

## Prompt
> Learn UX & information architecture from `old-report` and apply it to `frontend`.

Researched both apps' IA/UX with two parallel Explore agents, synthesized the gaps, and confirmed scope (kept the frontend's *better* global nav rail rather than copying old-report's header-only navigation). Applied the four agreed alignments:

## Work done
- **Unified the project "Overview"** — the frontend had fragmented it into an orphaned/broken `/overview` (`ProjectOverview`, unreachable + no active tab) plus `/summary` (a features table), both Tailwind-styled so they clashed with / broke inside the light report workspace. Rebuilt `FeatureSummary` as **one cohesive, workspace-styled page** matching old-report: eyebrow + project meta (Owner/Generated/Features/Groups) → **test-coverage rollup** (cases + avg coverage + result **donut** + per-result bars) → **status-total tiles** (Done/Testing/Info) → **features-by-group tables**. Ported the `.feature-summary*` / `.result-donut*` / `.rollup-*` CSS. Removed the dead `/overview` route + `ProjectOverview.tsx`; fixed the topbar tab active-state (`onReport = !onSummary`).
- **Project-scoped the workspace Roadmap & Bugs tabs** — they used to eject to the *global* unfiltered pages. Now they navigate to `/{bugs,roadmaps}?projectId=…&project=…`; `BugsBoardPage` and `RoadmapsPage` read the params, **filter to the project**, show a `BackLink` + "— {project}" heading, and new bugs/roadmaps **attach to the project** (added `defaultProjectId` to `CreateBugDialog`, `projectId` to roadmap create).
- **Wired up test-case import** — the `ImportTestCasesDialog` (+ `useImportTestCases` + i18n) was orphaned; added an **↥ Import** action in the Test Case Summary (via a new `onImport` prop, shown even when empty) that opens it for the current report.
- **Consistency polish** — workspace sub-pages now use the report-workspace styling (Overview) or navigate to the themed AppShell pages (scoped tabs), so nothing clashes in dark mode; the shared `BackLink` is used on the scoped views. (Kept the appropriate inline links in "not found" sentences.)

Verified with `tsc --noEmit`, `vite build`, and a headless-Chrome screenshot of the rebuilt Overview (eyebrow/meta/rollup-donut/status-tiles/groups).

---

## Prompt
> Preserve the features from `old-report` into this product (feature parity).

Built a full old-report → frontend **feature-parity matrix** (Explore agent), then split gaps into frontend-feasible vs backend-dependent. User picked all four frontend-only clusters + "run backend & verify" for the rest.

## Work done — frontend-only feature ports
- **Roadmap RICE bubble chart** (`RoadmapRiceChart.tsx`) — Effort×Impact scatter, bubble size = Reach, colour = Confidence (red→green), quadrant labels (Quick wins / Big bets / Fill-ins / Money pit), hover tooltip with the RICE breakdown. Added a **Board / RICE chart** view toggle to `RoadmapBoardPage`. Theme-aware SVG.
- **Sidebar drag-reorder features** — HTML5 DnD in `FeatureSidebar` (drag handle, drop indicator, `is-dragging`), reordering via the existing `useReorderReports` and moving across groups via `useUpdateReport({groupId})`. Ported the drag CSS.
- **Export & print** — `ProjectLayout` ⋯ menu now has **Export project (JSON)** (serialises project + reports) and **Download prompt** (a ready LLM prompt for generating importable reports); added a real `@media print` stylesheet so **Export PDF** hides chrome/edit-controls and flattens inputs to text.
- **Dashboard/project polish** — roadmap **phase-pill cards** on the dashboard, inline **environment switch** in the project-card menu (radio with ✓), and **Inbox All/Mentions/Assigned tabs** (client-side filter with counts).

## Backend verification (for the API-dependent features)
Inspected the NestJS backend controllers/DTOs. Verdict — the current backend **does not** support most of them, so they need backend development, not just frontend:
- ❌ **Bug↔case linking** — `CreateBugDto` has no `caseId`, no link route.
- ❌ **Configurable bug statuses / custom fields** — no endpoints (hardcoded enums).
- ⚠️ **Bug comments** — `bugs/:bugId/comments` GET/POST **exists** with `mentions[]` + `images[]` (URLs) in the DTO, but **no upload endpoint** and **no edit/delete** routes.
- ❌ **Screenshot upload** — no upload route (URL-only, already supported).
- ❌ **Public roadmap / bug share** — no controllers/routes (only public projects + testcases).
- ❌ **Duplicate / import project** — no `/:id/duplicate`, no `/projects/import` (report **test-case import exists** and is already wired).
- ❌ **Milestone individuals / Gantt** — no individual level in the DTO; no date fields for a Gantt.

Verified all four frontend clusters with `tsc --noEmit` + `vite build`.

---

## Prompt
> Build backend for top features — starting with test-case ↔ bug linking, then configurable bug statuses and comment edit/delete.

Kicked off real backend parity. Mapped the bugs vertical slice (Explore agent): the stack is **NestJS + Mongoose (schema-in-code, no migrations)** with clean/hexagonal layers (`application` domain/dtos/use-cases + `infrastructure` schema/repo + `presentation` controllers); the `projectId` filter is the end-to-end template.

## Work done — #1 Test-case ↔ bug linking (complete)
Added `caseId` + `caseLabel` + `reportId` to bugs, end-to-end, so a bug can be **created from and linked to a specific test case**:
- **Backend** — `bug.props.ts`, `bug.entity.ts` (create props + constructor + getters + `applyUpdate`), `bug.schema.ts` (fields; `caseId`/`reportId` **indexed**), infra `bug.repository.ts` (`toDomain`/`toDocument` + `findByTenant` filters `caseId`/`reportId`), `create-bug.dto.ts` / `update-bug.dto.ts` / `query-bug.dto.ts` / `bug.response.dto.ts`, `bug.mapper.ts`, `create-bug.use-case.ts` + `update-bug.use-case.ts`. Purely additive → existing bugs default the fields to `''`; **no migration needed** (Mongoose builds the index on boot).
- **Frontend** — `BugDto` + `BugQuery`/`CreateBugInput`/`UpdateBugInput` gained the three fields. **`CreateBugDialog`** takes `defaultCaseId/defaultCaseLabel/defaultReportId` and shows a "🔗 Linked to test case …" chip. The **Test Case Summary** now has a **Bugs column** (`ReportSections.tsx` `Testing`): a red `🐞 N` count chip (opens the board filtered to that case) + a dashed **+ Bug** button (opens the linked create dialog). `ReportView` fetches `useBugs({ reportId })`, builds a `bugCountByCase` map, and wires `onCreateBug`/`onOpenBugs`. `BugsBoardPage` reads `?caseId&case&reportId`, filters + scopes the heading ("Bugs for …") + prefills its create dialog. `BugDetailPage` shows a **Linked test case** row that links back to the case's report. New workspace CSS `.case-bug-cell/.case-bug-link/.case-bug-add`; i18n `bugs.linkedCase`/`bugs.forCase`.

Verified: backend `nest build` ✓, frontend `tsc --noEmit` + `vite build` ✓, and a headless-Chrome screenshot of the Bugs column (count chips + "+ Bug", empty rows show just "+ Bug").

**Next:** #2 configurable bug statuses, then #3 comment edit/delete.

## Work done — #2 Configurable bug statuses (complete)
Chosen scope (confirmed with the owner): **rename + recolor + reorder** the existing 5 columns per workspace — the workflow `key` stays fixed so filters/rollups keep working, no data migration.
- **Backend** — added `BugStatusConfig {key,label,color}` + `DEFAULT_BUG_STATUSES` (with default colors) to `bug.enums.ts`. Stored the ordered list on the existing **app-settings singleton**: entity (`bugStatuses` prop + default seeding + getter + `setBugStatuses`), infra schema (`bugStatuses` Mixed, `default: undefined` → domain seeds), infra repo (`toDomain`/`save`), DTOs (`BugStatusConfigDto` validated + `UpdateBugStatusesDto` + response field). New **`UpdateBugStatusesUseCase`** validates the provided keys are exactly the workflow set (each once) → guarantees no bug is stranded. Controller: `GET /settings/bug-statuses` (**any authenticated user** — drives the board), `PUT /settings/bug-statuses` (**admin**), and `GET /settings` now returns `bugStatuses`.
- **Frontend** — `useBugStatuses()` (shared read) + `useUpdateBugStatuses()`. The **Bugs board** columns now render from config (custom label + a colored dot), falling back to `DEFAULT_BUG_STATUSES`. **Bug detail** status dropdown + label read from config. New **admin editor** on `AdminSettingsPage` ("Bug board columns"): reorder ↑/↓, recolor (native color swatch), rename (input), the stable key shown in mono, Save + Reset to defaults. i18n `settings.bugStatuses*` etc.

Verified: backend `nest build` ✓, frontend `tsc --noEmit` + `vite build` ✓, headless-Chrome screenshot of the recolored/renamed board columns + the admin editor.

## Work done — #3 Comment edit/delete (complete)
Added edit + delete for bug-thread comments, authorized to the **author or an admin**.
- **Backend** — comment entity gained `updatedAt` (= createdAt until edited) + an `edit()` method; schema/props/mapper/response DTO carry it. Repo port+impl added `findById(tenantId,id)`, `update`, `delete`. New `UpdateCommentDto`, **`UpdateCommentUseCase`** + **`DeleteCommentUseCase`** (load → check tenant+bug+ownership, `role === ADMIN` overrides) returning `Result` with a sentinel message for the forbidden case. Controller added `PATCH :commentId` + `DELETE :commentId` (both `ADMIN`/`TESTER`), mapping ownership failures to **403 `ForbiddenDomainException`** and missing to **404 `EntityNotFoundException`**. Registered both use-cases in the activity module.
- **Frontend** — `useUpdateComment`/`useDeleteComment` + `UpdateCommentInput`; `CommentDto.updatedAt`. New **`CommentItem`** in `BugDetailPage`: hover-revealed **Edit** (inline `Textarea` + Cancel/Save) and **Delete** (confirm), shown only when `authorId === me || isAdmin`; an "· edited" marker when `updatedAt !== createdAt`. i18n `common.edit`, `activity.edited`, `activity.confirmDelete`.

Verified: backend `nest build` ✓, frontend `tsc --noEmit` + `vite build` ✓, headless-Chrome screenshot of the thread (edit/delete controls, "edited" marker, inline editor).

---

### ✅ All three top backend features shipped
`test-case ↔ bug linking` → `configurable bug statuses (rename/recolor/reorder)` → `comment edit/delete`. Every layer touched (Mongo schema → domain → DTO/use-case → controller → React), builds green, each verified with a screenshot. **Restart the backend** once to pick up the new Mongo indexes (`bugs.caseId/reportId`); no data migration needed (all fields are additive with safe defaults).
