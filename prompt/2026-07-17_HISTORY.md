# 2026-07-17 — History

## Prompt
> `roadmaps/:id` backlog item modal — change the heading to the backlog item's name, and allow editing it inline.

The modal's heading was a static `Edit item` / `Add item`, with the real title in a separate `h-11` input at the top of the form body. Promoted the name **into** the heading and deleted the body field (it would have been a duplicate).

## Work done
- **`components/ui/Dialog.tsx`** — `title` widened `string` → **`ReactNode`** so a dialog can render a custom heading. String titles keep the exact old markup, so **every existing call site is untouched**. A node title can't supply the dialog's accessible name, so added **`titleLabel`** → rendered in an `sr-only` `DialogPrimitive.Title` (Radix requires a Title; without it the dialog loses its accessible name). Close button gained `shrink-0` so a long title can't squash it.
- **`RoadmapItemDialog.tsx`** — heading is now an `Input` styled as the heading (`text-base font-semibold`, `border-transparent` → `hover:border-input` / `focus-visible:border-input`, so the edit affordance appears on hover with **no layout shift** — the border is always there, just transparent). `-ml-2 px-2` keeps the text optically aligned with where the old `<h2>` sat while the hover background bleeds into the header padding.
  - The heading renders in the Dialog **header**, i.e. outside `<form id="rm-item">` — so it carries **`form="rm-item"`**, which keeps it in the form's submit + constraint validation (`required` still blocks Done and focuses the field) and makes **Enter save**. Same association trick the footer's Done button already used.
  - `titleLabel` still resolves to `Edit item`/`Add item`, so the dialog's screen-reader name didn't regress.
- **i18n** — new `roadmaps.itemTitlePlaceholder` ("Untitled backlog item"); the heading keeps `roadmaps.itemTitle` as its `aria-label`.

---

## Prompt
> Backlog item — I want to pick an existing task. The picker form allows searching by name or taskId.

Linking is just a `PATCH /tasks/:id` that writes the same flat link the inline-add already writes (`roadmapId` + `roadmapItemId` + denormalized `roadmapItemLabel` + `projectId`), so this is mostly a UI + search change.

## Work done
- **`features/tasks/components/PickTaskDialog.tsx`** (new) — search box (debounced 300ms, no debounce helper existed) → `useTasks({ search })`; rows show status dot, title, **the task id** (first 8 chars, full id on hover), and the item it's currently linked to. Click → `useUpdateTask` writes the link, `['tasks']` invalidates so the panel refreshes, dialog closes. Tasks already on this item are filtered out. Mutation errors render inline rather than failing silently.
- **`TaskPanel.tsx`** — **"Link existing"** button (outline, `Link2`) beside the existing Add row; the row now wraps (`flex-wrap` + `flex-1 basis-40`) so it holds two buttons on mobile.
- **Backend `task.repository.ts`** — `search` only matched `title`/`description`, so **searching by id could never have worked**. Added **`{ _id: re }`** to the `$or` (`_id` is a uuid **string**, so a regex filter is valid). Also **escaped the regex input** — the search box takes free text, and an unbalanced `(` would otherwise throw on `new RegExp`.
- **i18n** — `tasks.pick`/`pickTitle`/`pickSearch`/`pickEmpty`/`pickNone`/`pickLinkedTo`/`pickUnlinked`/`pickMoveHint`/`pickAction`.

### 🐛 Pre-existing bug found + fixed
`useTasks` requested **`limit: 200`**, but the backend's shared `PaginationDto` caps `limit` at **`@Max(100)`** — so `GET /tasks` was returning **400** and the Tasks panel could never load. Every other hook in the app already used `limit: 100`. Fixed `features/tasks/api.ts` → `100`. **Confirmed against the running API:** `limit=200` → `HTTP 400`, `limit=100` → `HTTP 200`.

### Decision taken (flagged)
The picker lists **tenant-wide** tasks, including ones already linked to *another* backlog item — picking one **moves** it here. Each row shows its current link, and `tasks.pickMoveHint` says so. Chosen because it's visible and reversible, and because scoping to a project would stop a pasted id from resolving.

---

## Prompt
> Backlog item modal — allow full-page zoom; the zoom button sits next to the X.
> The modal's full-page state should be remembered in localStorage, per feature (e.g. roadmap, task, …).

## Work done
- **`components/ui/Dialog.tsx`** — single opt-in prop **`fullscreenKey?: string`**: passing it both **shows the maximize/restore toggle** (lucide `Maximize2`/`Minimize2`, left of the X) and **names its localStorage slot**. Folding the two into one prop makes the invalid combo (toggle on, nowhere to persist) unrepresentable.
  - Persisted at **`ph_dialog_full:<key>`** as `'1'`/`'0'`, following the existing `ph_nav_collapsed` convention in `Sidebar.tsx` (same `ph_` prefix, same `'1'`/`'0'`, same lazy `useState` init). Read on mount — the dialog is conditionally mounted, so it re-reads on every open and **reopens as it was left**.
  - Maximizing appends `h-[calc(100dvh-1rem)] max-h-none w-[calc(100%-1rem)] max-w-none` **after** the caller's `className` in `cn(...)`, so tailwind-merge resolves it last and it overrides `max-w-5xl` without `!important`.
  - `aria-pressed` + a label that flips Maximize/Restore. Kept i18n-free to match the primitive's own precedent (`aria-label="Close"`, and `Combobox` taking English string defaults rather than importing `t`) — **worth revisiting** if we want primitives on i18n.
- **`RoadmapItemDialog.tsx`** — opted in with `fullscreenKey="roadmap-item"`. Any other modal can opt in with its own key and get an independent memory; I didn't add toggles to modals you didn't ask about.

---

## Prompt
> Backlog item modal — move Status & Difficulty to the top, above Start date.
> Backlog item modal — remove the Phase dropdown, it isn't needed.

## Work done (`RoadmapItemDialog.tsx`)
- Sidebar order is now **Status · Difficulty** (2-col grid) → Start date → Assignees → RICE → Progress.
- Deleted the old bottom `Phase / Status / Difficulty` block entirely — Status/Difficulty moved up and Phase is gone, leaving it empty.
- **`form.phase` is still in the data model**, just no longer editable here: it's the board column, set by `defaultPhase`/`columns[0].key` on create and by drag-and-drop on the board. The `columns` prop is still needed (task label + `emptyItem`), and `roadmaps.phase` stays in i18n — `RoadmapRiceTable` still renders it as a column header.

---

---

## Prompt
> Status uses a colour dropdown. → difficulty as well.

Neither `RoadmapItemStatus` nor `RoadmapDifficulty` had a colour map (only `_LABEL`), so both were added — following the house rule every other map states verbatim: **semantic shadcn tokens, no new brand colours**.

## Work done
- **`types/enums.ts`** — new `ROADMAP_ITEM_STATUS_COLOR` (Idea = `--muted-foreground` · Planned = `--primary` · In progress = `--info` · Done = `--success`) and `ROADMAP_DIFFICULTY_COLOR` (Easy = `--success` · Medium = `--warning` · Hard = `--destructive`). In progress/Done deliberately reuse the exact values from **`TASK_STATUS_COLOR`** so the same state reads the same across features; difficulty uses the escalating green→amber→red ramp **`OKR_STATUS_COLOR`** already uses. All five tokens exist in `tailwind.css` as HSL triplets with light **and** dark variants.
- **`RoadmapItemDialog.tsx`** — local `DotLabel` (dot + text, dot `aria-hidden` so the label still carries the meaning) passed as the `Select` option `label`. **No change needed to `Select`**: `SelectOption.label` is already `ReactNode`, and `SelectMenuItem` wraps children in Radix `ItemText`, which Radix mirrors into the trigger — so the dot shows in the open list *and* the closed picker.
- **`components/ui/Select.tsx`** — the doc comment claimed colored items required dropping to `SelectMenu`; that's stale now that it has a real consumer. Reworded: node labels are supported here, `SelectMenu` is for grouping.

Not affected by the `.report-workspace` token shadowing (where `--primary` is a full `hsl()` and would break `hsl(var(--primary))`): the roadmap dialog portals to body, so the dots resolve at app scope.

**Left alone:** `RoadmapRiceTable`'s Status column is still plain text (it already dots the *Column*) — the ask was the dropdown; offered as a follow-up.

---

## Prompt
> The X and zoom buttons in the modal must have a rectangle, like `<button class="topbar-btn ghost topbar-close">`.

The two header buttons were **bare icons** (`opacity-80` → `hover:opacity-100`, no box). The reference — the project workspace's topbar close — is a **28px square ghost button with a 16px icon**: transparent background *and* transparent border at rest, subtle background + stronger text on hover.

## Work done (`components/ui/Dialog.tsx`)
Rather than hand-roll the box, both buttons now use the house **`Button`** (`variant="ghost" size="icon"`) — the UI abstraction layer, which already matches the reference: ghost = transparent → `hover:bg-accent hover:text-accent-foreground` (the token equivalent of the reference's `--bg-subtle`/`--text`), and its cva base carries **`[&_svg]:size-4`**, giving the 16px icon for free (so the explicit `size-5`/`size-[18px]` on the icons are gone). Shared `HEADER_BTN` = `size-7 shrink-0 text-muted-foreground` → the 28px box. `DialogPrimitive.Close` now wraps its `Button` with **`asChild`** so Radix merges its close handler onto the real button.

**Checked, not assumed:** `Button`'s `size="icon"` is `h-9 w-9` (36px), so `size-7` only wins if tailwind-merge understands the `size-*` ⇄ `w-*`/`h-*` conflict. Ran the installed lib (**tailwind-merge 3.6.0**): `twMerge('h-9 w-9','size-7')` → `size-7` ✓, with `[&_svg]:size-4` preserved. No import cycle — `Dialog` imports `./Button` directly, and `Button` imports neither.

---

## Prompt
> As product & admin, I can edit roadmap item.
> → (clarified with a screenshot) "roadmap item" means **this card's info** on `/roadmaps` — include: title.

**First read this as the backlog item inside a roadmap board and verified the wrong thing** (that path already worked for admin/product; tester too, developer 403 — owner reviewed the matrix and chose "change nothing"). The screenshot corrected it: **"roadmap item" = the roadmap card on the `/roadmaps` list** ("roadmap 1 · 3 items · 17h ago").

Real gap: `RoadmapsPage` cards were **click-to-navigate only** — the page's sole dialog was *create*, so **nobody could rename a roadmap**, product/admin included. Backend + hooks were already fully in place and unused: `PATCH /roadmaps/:id` ("Update roadmap meta", accepts title + description) gated `@Roles(ADMIN, TESTER, PRODUCT)`, and **`useUpdateRoadmap` existed as dead code with zero call sites**. So this was UI-only.

## Work done (`RoadmapsPage.tsx` + i18n)
- **Card edit affordance** — a ghost `Pencil` icon button (`size-7`, matching the dialog header buttons) pinned top-right of each card, gated on `canWrite`. `stopPropagation` on click so it opens the editor instead of navigating to the board; title gets `pr-8` so long names don't run under it. Revealed on `group-hover`/`focus-visible`, but **`max-sm:opacity-100`** so it's reachable on touch, where there is no hover (the roadmap board's toolbar has this gap; not regressed here).
- **One dialog, two modes** — reused the existing create dialog rather than adding a second: `editing: RoadmapDto | null` switches the heading (Create/Edit roadmap), the submit label (Create/Save) and the `loading` source. Editing PATCHes and **stays on the list** (the card updates in place via the existing `['roadmaps']` invalidation); creating still navigates into the new board.
- Fixed a hardcoded `"Description"` label → `t('roadmaps.description')`, and an odd two-imports-on-one-line. New key `roadmaps.edit`.

**Gate = `canWrite` [admin, tester, product]**, mirroring `PATCH /roadmaps/:id`'s own `@Roles` — per the house rule that `useAuth` gates mirror the backend matrix. Product & admin can edit (the ask); tester keeps the access the owner had just chosen not to remove. Gating on `canManageDelivery` [admin, product] would have matched the literal wording but left testers able to *create* a roadmap they couldn't *rename*.

**Verified on the running API** (throwaway tenant, one user per role): `PATCH /roadmaps/:id` → **product 200**, **admin 200**, both reading back the new title + description; the list card's `itemCount` still resolves.

---

## Prompt
> `roadmaps/:id` — when dragging to change a backlog item, the pool it will be placed into doesn't need a border colour. (The border colour being the border at the Next / Later / Done column.)

`DroppableColumn` ringed the **whole column** (`isOver && 'ring-2 ring-primary'`) while dragging over it. That dated from before the drop placeholder existed; now that the dashed, card-sized box marks the **exact landing slot**, ringing the entire column on top of it is redundant — and it fights the column's own identity colour (Now/Next/Later/Done), which is the accent the eye should track.

## Work done (`RoadmapBoardPage.tsx`)
Dropped `isOver` + `ring-2 ring-primary` from `DroppableColumn` — and `transition-shadow`, which only existed to animate that ring (so the `cn()` collapsed to a plain string; `cn` is still used 3× elsewhere in the file). **`setNodeRef` stays**, so the column is still a drop target and dropping on its empty area still appends there. Only the visual went away; the dashed placeholder + the lifted `DragOverlay` card remain as the drop cues.

**Left `BugsBoardPage` alone** — it has the same ring, but no drop placeholder was ever built for it, so the ring is its *only* drop indicator. Removing it there would leave nothing.

---

## Prompt
> Bugs and Tasks use the kanban layout like roadmap. Tasks additionally supports a list view grouped by status.

Planned first (multi-file + an architectural fork). Owner picked: **extract one shared Kanban and migrate all three**, and **Board as the `/tasks` default**.

Exploration found this is **frontend-only** — no backend work at all:
- `BugStatusConfig` is `{key,label,color}` — **structurally identical to `RoadmapColumn`**; task statuses map to it via the existing `TASK_STATUS_LABEL`/`_COLOR`. One `KanbanColumn` type fits all three.
- `useSetTaskStatus()` / `PATCH /tasks/:id/status` already existed (used by `TaskPanel`).

**The design crux:** roadmap **reorders within a column** (persists the items array, order = index); bugs/tasks only **move between columns** (a status write). Resolved with one callback carrying enough for both: `onMove(id, toColumn, overId)` — `overId` is the exact slot, or `null` for an empty-area drop. Roadmap splices with it; bugs/tasks ignore it.

## Work done
- **`components/KanbanBoard.tsx`** (new, 247 lines) — generic over `<T>` with render props (`renderCard`, `renderCardToolbar`, `renderColumnFooter`, `renderBoardEnd`). The roadmap's DnD was lifted **verbatim**, not redesigned: BOARD layout, `DroppableColumn` (no hover ring), card-as-droppable for precise slots, pointer-first collision detection, `dragHeight` → card-sized dashed placeholder, `DragOverlay`, colour-dot column header, and **orphan absorption** (first column takes items whose column was deleted — Bugs needs this too, its statuses are tenant-configurable). Presentational: no feature imports.
- **`RoadmapBoardPage`** (-120 lines) — migrated first (highest regression risk) and verified before touching the others. Splice logic moved **unchanged**. Sorting insight: sorting the whole array by RICE then filtering per column == sorting each column, so `boardItems` goes straight in; the splice still targets the raw array, preserving the old behaviour.
- **`BugsBoardPage`** (-60 lines) — gains full-height, drop placeholder, mobile stacking, `bg-card` columns. Filters/`+ New bug` kept.
- **`MyTasksPage`** (+board) — `KanbanBoard` + a new `TaskCard`; the original grouped list extracted to `TaskList` **unchanged**; `?view=list` toggle mirroring the roadmap's. Click → the task's roadmap (no task detail page exists).
- **`AppShell`** — `/bugs` + `/tasks` join the roadmap as full-bleed **and** full-height. Kept **pathname-only** (no `?view=` parsing): non-board views scroll inside the shell, exactly as roadmap already did for chart/table.
- **`PageHeader`** — added `shrink-0`; it's a flex child on the new full-height pages and would otherwise squash. Inert on every other page (non-flex parents).

**Net: `@dnd-kit` is now imported in exactly one file** (was three). 398 deletions vs 276 insertions.

**Verified on the running API** (throwaway tenant): `PATCH /tasks/:id/status` todo→in-progress→done all 200 + read back; `PATCH /bugs/:id/status` → 200; `PUT /roadmaps/:id/items` reorder **A B C → B A C** with a phase change, order preserved.

**Not driven in a browser** (no automation this session) — the drag interactions themselves are typecheck/build-verified only and need eyeballing: roadmap reorder + placeholder tracking, click-vs-drag, and all three at mobile width.

---

## Prompt
> `bugs/:id` detail — status & severity use a colour dropdown.

Same treatment as the backlog item modal. **Status colours are read from the tenant's board config** (`useBugStatuses() ?? DEFAULT_BUG_STATUSES` — `c.color`), not a hardcoded map, so the dots match the columns on `/bugs` exactly, including any recolouring. Severity reuses the existing `BUG_SEVERITY_COLOR`.

Second consumer for the dot label ⇒ extracted `RoadmapItemDialog`'s local `DotLabel` into **`components/ui/DotLabel.tsx`** + the barrel, and pointed the roadmap dialog at it. No new colours.

---

## Prompt
> Bug and task detail: ESC goes back a step.
> → (clarified: no task detail page existed) owner chose **also add a task detail page**.

### `lib/useEscapeBack.ts` (new)
A naive document `keydown` listener would fire **while Escape is closing a dropdown** and navigate away — the page is full of Radix Selects/Comboboxes/MultiSelects. So the hook skips when:
- an overlay owns Escape — `[data-radix-popper-content-wrapper], [role="dialog"]` is in the DOM,
- the target is a field (`input/textarea/select/[contenteditable]`),
- or `e.defaultPrevented`.

The overlay check reads the **DOM, not the event**, which holds regardless of listener order: React unmounts on a later tick, so the layer is still mounted while the handler runs. Wired into `BugDetailPage`.

### `features/tasks/TaskDetailPage.tsx` (new) + `/tasks/:taskId`
Mirrors the bug detail layout (content + sticky meta sidebar): blur-to-save title & description, colour-dot status `Select`, assignee (`Combobox` for admin/product, "Assign to me" toggle otherwise — the same split `TaskPanel` uses), backlog-item link, created-by, delete. `useEscapeBack()` wired.
- **`useTask(id)`** added to `features/tasks/api.ts`.
- **Bug found while adding it:** the task mutations invalidated only `['tasks']`, which **does not prefix-match `['task', id]`** — a detail page would never refresh after an edit. Replaced with a shared `useInvalidate()` hitting both keys (mirrors `features/roadmaps/api.ts`).
- Task cards + list rows now open the detail page instead of jumping to the roadmap (the roadmap is still one click away in the sidebar).
- New keys: `tasks.notFound` / `confirmDelete` / `createdBy`.

**Verified on the running API:** `GET /tasks/:id` ✓, `PATCH` title+description ✓ 200 and reads back, `PATCH /status` ✓ 200, bogus id → **404** (the page's not-found state).

---

## Prompt
> Settings layout like [screenshot], with a left menu.

`AdminSettingsPage` was one scrolling column of three stacked cards. Restructured to the reference: **← Dashboard** back link, title + new subtitle, then a **left menu beside the content**.
- `TABS` drives the menu (Bug statuses `ListChecks` · API keys `KeyRound` · Webhooks `Webhook`), each entry naming its `Section` component — so the three existing sections were **reused untouched**, only the shell changed.
- Active item `bg-accent` + `aria-current="page"`; open section persisted in **`?tab=`** (first tab kept out of the URL), the same pattern as the boards' `?view=`.
- **Responsive:** a horizontally-scrolling tab strip on mobile, a fixed 56-wide vertical menu from `md` up.
- Dropped an already-unused `user` from the `useAuth()` destructure while in the file.

---

## Prompt
> Bugs & tasks also allow adding custom columns, like roadmaps.

**Planned + owner decision captured; implementation deferred to the next session** (queued after the work above).

The investigation surfaced why this isn't a copy of the roadmap feature: **roadmap columns are free because `phase` carries no meaning** (an item's `status` is a separate field). For bugs and tasks the **column *is* the status**, and it drives real logic:
- `TaskStatus.DONE` → `TaskPanel`'s "N of M done" rollup + progress bar, and strikethrough in 3 places.
- `BugStatus.RESOLVED`/`CLOSED` → the dashboard's "open bugs" stat.

Plus hard constraints: both `status` fields are **mongo `enum`-locked** and `@IsEnum`-validated, and `BugStatusConfig.key` is typed to the enum ("the fixed workflow value" — today admins can only relabel/recolour/reorder).

**Owner chose: built-ins stay (renameable, not deletable) + add custom columns alongside** — so the rollups and dashboard keep working off the surviving built-in keys.

Remaining plan: drop the mongo `enum` + `@IsEnum` on both `status` fields; widen `BugStatusConfig.key` to `string`; add a tenant-level **`taskStatuses`** settings field mirroring `bugStatuses` (it has none today); generalise `RoadmapColumnsDialog` into a shared columns dialog; add "+ Add column" to both boards. `KanbanBoard`'s orphan absorption already covers items left on a deleted column.

---

## Prompt
> Make the sidebar more compact, text a bit smaller.

Tightened `components/Sidebar.tsx` throughout (the only file with the width — nothing else referenced it, so it's self-contained). Sizing only; **no colour changes**, all existing `sidebar-*` tokens.

| | before | after |
|---|---|---|
| Width | 248px | **216px** |
| Collapsed | 64px (`md:w-16`) | **56px** (`md:w-14`) |
| Brand row | `h-14` (56px), `text-lg` mark | `h-12` (48px), `text-sm` + `text-base` mark |
| Nav | `p-3`, `gap-4` | `p-2`, `gap-3` |
| Nav item | `px-2.5 py-2`, `text-sm` (14px), icon 18 | `px-2 py-1.5`, **`text-[13px]`**, icon 16 |
| Group heading | `text-[11px]` | `text-[10px]` |
| Inbox badge | `h-5 min-w-5`, `text-xs` | `h-4 min-w-4`, `text-[10px]` |
| Footer | `p-3`, avatar 30px, name 14 / role 12 | `p-2`, avatar 28px, name 13 / role 11 |

Checked the collapsed rail still fits: 56px − `p-2` (16) − `px-2` (16) = 24px for a 16px icon ✓; footer avatar is an exact 28px fit inside `p-2` + `px-1.5` ✓.

---

## Prompt
> Bugs and tasks support more filter types. Use the filter component from `old-frontend/src/components/filters` — **copy this component folder** into this project.

**Couldn't copy — surfaced the blocker first** ([[old-to-new-frontend-migration]], 4th time). The folder *looks* portable (it's shadcn-shaped: imports `../ui/button`, `checkbox`, `dropdown-menu`), but `old-frontend/tailwind.config` sets **`prefix: "tw-"`** — every class is `tw-h-4 tw-w-4`, so a copy renders **completely unstyled** here. Plus the old brand palette (`tw-text-brand-primary-500`) and deps we don't have (`react-i18next`, `@phosphor-icons/react`, `utils/queryBuilder`, `@/utils/modalUtils`). That's 2,324 lines of non-compiling, unstyled code.

Owner chose: **lean native rebuild**, **multi-select with backend `$in`**, filters = Bugs (Status/Severity/Assignee/Project) + Tasks (Status/Assignee/Backlog item/Project).

## Backend — multi-value filters
- **`shared/utils/query-array.util.ts`** (new) — `TransformQueryArray()` normalizes `?x=a`, `?x=a,b` and `?x=a&x=b` to `string[]`; blank → `undefined`. **Backwards compatible**: a single value becomes a 1-item array, so `$in` ≡ the old equality match for existing callers.
- `query-bug.dto` / `query-task.dto` — status, severity, assigneeId, projectId, roadmapId, roadmapItemId → arrays with `@IsEnum(..., { each: true })`; validation still rejects junk (`?severity=bogus` → **400** ✓).
- Both repositories → `{ $in: [...] }`. Bug search also gained the **regex escaping + `_id` match** the task repo already had.

### Two real bugs found by verifying against the API rather than trusting the code
1. **axios would have silently broken every multi-select.** Axios 1.x serializes arrays as `?status[]=a` by default, and this API's query parser doesn't read the bracket form — `?severity[]=low&severity[]=medium` returned **all 4 bugs** (filter dropped). Fixed globally in `lib/api.ts` with `paramsSerializer: { indexes: null }` → repeated keys (`?status=a&status=b`), which I'd verified works. No existing caller sends arrays, so nothing else changes.
2. **"Unassigned" filtered nothing.** Unassigned is stored as `''`, but `?assigneeId=` is indistinguishable from "filter not set" (and the transform drops blanks) — it returned **all 3** bugs. Added a documented sentinel (`UNASSIGNED_QUERY = '__unassigned__'` + `resolveAssignees()`) that the repos map back to `''`; the frontend exports the matching `UNASSIGNED`.

## Frontend — `components/FilterMenu.tsx` (new, ~190 lines)
Keeps the old component's **API shape** (categories → options, multi-select, per-category search, clear-all, count badge on the trigger) and drops the plumbing react-query already does (`apiEndpoint`/`searchEndpoint`, 3-level `subOptions`, manual-save variant). Built on the app's `DropdownMenu` + `Sub` menus, `Badge`, tokens — status/severity dots reuse the board colours, **no new colours**.
- Two Radix gotchas handled: `onSelect={e => e.preventDefault()}` on the checkbox items so the menu stays open across several picks, and `onKeyDown={e => e.stopPropagation()}` on the search box so the menu's typeahead doesn't swallow every keystroke.
- **`BugsBoardPage`** — replaced the single-value severity `Select`; a `?projectId=` in the URL still scopes the board (the Project category is then hidden as redundant).
- **`MyTasksPage`** — filter menu in the header; `mine` stays the base filter. Backlog-item options are flattened across roadmaps and prefixed (`Roadmap · Item`) so same-named items stay distinct.

**Verified on the running API:** single-value back-compat ✓, CSV ✓, repeated keys ✓, `severity=high,critical` → 2 ✓, project+severity combined → 1 ✓, `__unassigned__` → exactly the 2 unassigned ✓, `__unassigned__`+me → 3 ✓, `?severity=bogus` → 400 ✓.

---

## Prompt
> Make the filter widget equal height with the bugs search input.

The filter trigger was `Button size="sm"` (**h-8**, `text-xs`) sitting between two **h-9** `text-sm` controls — the search `Input` and the `+ New bug` button.

Didn't hardcode `h-9`: on `MyTasksPage` the same widget sits beside `size="sm"` controls where h-8 is correct. Added a **`size?: 'sm' | 'default'`** prop to `FilterMenu` mirroring `Button`'s (default `'sm'`, so Tasks is unchanged), and `BugsBoardPage` passes `size="default"` → h-9 + `text-sm` + `px-4`, matching both neighbours exactly rather than just the height.

---

## Verification
Frontend `tsc --noEmit` ✓ + `vite build` ✓ (2103 modules). Backend `tsc --noEmit` ✓.

Drove the **real API** (`localhost:3000/v1`) against a throwaway registered tenant (register creates its own tenant, so no existing data was touched):
| Check | Result |
|---|---|
| `search=<full task uuid>` | ✓ 1 hit — **new**, id search previously impossible |
| `search=<8-char id prefix>` | ✓ 1 hit |
| `search=passkey` (name) | ✓ 1 hit — existing behavior intact |
| `search=(` (regex-unsafe) | ✓ 0 hits, no 500 — escaping works |
| `limit=100` / `limit=200` | ✓ 200 / ✗ **400** — confirms the bug above |
| `PATCH` link → `GET ?roadmapItemId=` | ✓ label + id written, panel query returns the task |

**Not driven in a browser** — no browser automation in this session, so the visual bits (inline heading hover/focus, the maximize toggle + its localStorage persistence, the reordered sidebar) are typecheck/build-verified only and worth eyeballing. No new colours: everything is on existing tokens (`border-input`, `bg-accent`, `border-primary`, `text-destructive`).
