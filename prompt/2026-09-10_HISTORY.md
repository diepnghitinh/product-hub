# 2026-09-10

## Prompt

> thôi bỏ viewmode đi, viewmode render bị lỗi
> edit ko bị
> giúp tui bỏ viewmode trong tất cả ticket details

(Kèm 2 ảnh: view mode của QC-120 hiện nguyên `<p><b>…</b>` dưới dạng chữ thô; edit mode render đúng.)

## Đã làm

Bỏ hẳn cặp nút **Edit / "Read & translate"** ở description của ticket detail — mở ra là editor luôn,
không còn read view.

- **`features/issues/IssueDetailMain.tsx`** (task + bug + peek drawer): xoá `useEditToggle`, luôn
  render `RichTextEditor`; `DescriptionTemplates` không còn `actions={edit.button}`, và `onApply`
  gọi thẳng `seed.apply` (không cần `edit.edit()` để mở editor nữa).
- **`features/roadmaps/components/RoadmapItemDetail.tsx`** (backlog item): sửa y hệt.

## Không đụng tới

- **`components/EditToggle.tsx`** giữ nguyên — doc page (`DocPageEditor`) vẫn dùng, và ở đó nút này
  đi theo chiều ngược lại (mở ra là editor, bấm để sang read & translate).
- Nhánh **read-only cho người không có quyền sửa** (`canWrite === false`, public share) vẫn là
  `RichText`. Lỗi render ở ảnh 1 là lỗi của `RichText`/nội dung đã lưu, nên nhánh này về lý thuyết
  vẫn dính — cần xem riêng nếu có ticket public bị.

`tsc --noEmit` sạch (chỉ còn lỗi cũ do chưa cài `vitest`/`@types/node`). **Chưa thử tay trên browser.**
