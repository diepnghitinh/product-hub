# 2026-07-16 — History

## Prompt
> Bring basic components (text input, dropdown, checkbox, …) from `old-frontend/src/components` into `frontend/src/components`.

Compared both UI layers and surfaced a blocker before copying anything: the two frontends are **different stacks** — `old-frontend` = **MUI Joy** + `tw-`-prefixed Tailwind + a bespoke color system (`brand-primary-500`, `--Base-White`, `@/constants/colors.constant`); `frontend` = **Radix + shadcn/ui** + unprefixed Tailwind + design tokens (`border-input`, `bg-primary`, …). A literal copy would neither compile nor match the palette (violating the "keep UI colors consistent" rule). Also noted the named basics (Input, Select/select-menu, checkbox, textarea, switch, label) **already exist** in the new frontend. Confirmed scope with the owner → rebuild the genuinely-missing basics **natively in shadcn style**. Owner picked all four.

## Work done — 4 new form primitives (all in `frontend/src/components/ui/`)
- **`radio-group.tsx`** — canonical shadcn RadioGroup/RadioGroupItem on **`@radix-ui/react-radio-group`** (added the dep; consistent with checkbox/switch). Tokens: `border-primary`, `fill-primary`, `ring-ring`.
- **`MultiSelect.tsx`** — multi-value picker reusing the existing **dropdown-menu + Badge**. Trigger shows removable chips (× uses `stopPropagation` so it doesn't toggle the menu) or "N selected"; menu is checkbox items that keep it open on select (`onSelect preventDefault`). Controlled `value: string[]`.
- **`Combobox.tsx`** — searchable single-select on **Radix Popover** (already installed — no `cmdk` dependency added). Token-styled trigger + search box + filtered list, with keyboard nav (↑/↓ move, Enter select, Esc close) and portal rendering.
- **`TagInput.tsx`** — free-text → removable chips (Badge). Enter/comma commits, Backspace-on-empty removes last, blur commits; `maxTags` + case-insensitive `dedupe`. Styled to match `Input`.

All four use the house conventions: `forwardRef`/`cn`, `@/` alias, lucide icons, `aria-invalid` → `border-destructive`, brand `--primary` for selected states.

## Wiring
- **Barrel export** (`ui/index.ts`) — added `RadioGroup`/`RadioGroupItem`, `Combobox`, `MultiSelect`, `TagInput` (+ their types).
- **Living style guide** (`pages/DesignPatternsPage.tsx`) — new **"Advanced form controls"** section (Combobox / Multi-select / Tag input) and a **"Radio group"** section, all interactive, so they're documented alongside the rest of the shadcn library.

Verified: `tsc --noEmit` ✓ (whole project) and `vite build` ✓ (2080 modules, no errors — confirms the new radio-group + popover imports bundle). No new colors introduced; everything is on existing tokens. `frontend/dist` is gitignored.

---

## Prompt
> Apply the new components to the similar existing components currently in the app.

Swept the app for hand-rolled patterns the new primitives do better. Kept short **enum** selects native (status/severity/role/phase/difficulty/milestone-status — a Combobox there is overkill); applied the new components only to the genuine wins (people pickers + a native multi-select). No radio/tag-input candidates exist yet, so those stay available for future use.

## Work done — 3 swaps (behavior-preserving)
- **Mentions "notify" picker** (`BugDetailPage`) — replaced a native **`<Select multiple>`** (ctrl-click, no chips — the worst UX in the app) with **`MultiSelect`** (removable chips, checkbox menu). New i18n `activity.notifyPlaceholder`.
- **Bug assignee** (`BugDetailPage`) — `<Select>` over up to 100 users → **`Combobox`** (type-to-search). Unassigned modeled as an `''` option, so `save({ assigneeId })` is unchanged.
- **Test-case owner** (`CaseEditDialog`) — `<Select>` over project users → **`Combobox`**, preserving the "— Unassigned —" empty option and the legacy unknown-owner passthrough (`!ownerKnown`).

Left `OwnerSelect` (reports `TestingTable`) as-is — it's already a solid Radix `SelectMenu` in a dense table; converting it is a larger, riskier change I flagged as optional follow-up.

Verified: `tsc --noEmit` ✓ + `vite build` ✓. Only people-pickers/multi-select changed; all enum dropdowns untouched.

---

## Prompt
> Bring `old-frontend/.../date-pickers` (the single one) into `frontend/src/components`, and use it in Project detail → "Reported".

The old suite is heavy **MUI `Popper` + `dayjs` + phosphor** with a 17KB modal — none of those libs exist in the new frontend, so (as before) rebuilt natively rather than copied.

## Work done — new `DatePicker` (`components/ui/DatePicker.tsx`)
Single date picker: token-styled trigger over a **hand-rolled month calendar** in a Radix Popover — **no date library**. Reads/writes plain **`YYYY-MM-DD`** strings (timezone-safe local parse/format), so it's a drop-in for a native `<input type="date">`. Month nav (‹/›), weekday header, 6×7 grid with outside/today/selected/disabled states, optional `min`/`max`, `clearable` (× + Clear), and a **Today** shortcut. Exported from `@/components/ui` and added to the Design Patterns gallery.

## Wired into Project detail → Reported (`reports/ReportView.tsx`)
Replaced the native `<input type="date">` in the report meta with `<DatePicker>` (`onChange` still writes `{ reported }` via `useUpdateReport`). Data format unchanged.

### Theming gotcha handled — `.report-workspace` token shadowing
The report workspace **redefines** shadcn tokens on its root to stay always-light: some as **valid HSL triplets** (`--input/--popover/--accent/--ring/--muted-foreground/--foreground` ✓) but `--primary`/`--border` as **full `hsl()`/hex** — which *breaks* `bg-primary`/`border-border` for anything rendered **inside** the workspace. So:
- The **trigger** (renders in-place, inside the workspace) uses only workspace-valid tokens — `border-input`, `ring-ring`, `text-foreground`/`muted-foreground`; **no `bg-primary`**.
- The **calendar** is **portaled** (escapes to app scope, where `bg-primary` etc. are valid) — exactly how the existing `OwnerSelect`/`SelectMenu` pickers already behave in the workspace, so no new inconsistency.

Verified: `tsc --noEmit` ✓ (caught + fixed a `boolean | null` on the day button) + `vite build` ✓.

---

## Prompt
> All dropdowns must be non-native — use the component ones, not native `<select>`. (Interrupted the Projects→Testing rename to do this first.)

Standardised every dropdown on the non-native Radix `SelectMenu`. Rather than rewrite ~11 call sites to the verbose compound API, I **rewrote the `Select` component itself to be non-native**: it now takes an `options` array + `onValueChange` and renders `SelectMenu` under the hood (real portaled listbox, never an OS `<select>`), bridging `''` ⇄ a sentinel so call sites can still model an "All"/"Unassigned" option. So every `<Select>` in the app became non-native automatically, with terser call sites.

## Work done
- **`components/ui/Select.tsx`** — rewritten: native `<select>` → options-based wrapper over `SelectMenu`. Exports `SelectOption`/`SelectProps`. Updated the Design Patterns demo to the new API.
- **Migrated 8 enum `<Select>` call sites** (native `<option>` children + `onChange` event → `options` + `onValueChange`): RoadmapItemDialog (phase/status/difficulty), AdminPeoplePage (role ×2), BugsBoardPage (severity filter, `''`=all), CreateBugDialog (severity), BugDetailPage (status/severity), MilestoneDetailPage (status).
- **Report workspace raw `<select>` ×3** → non-native: the two **owner** pickers now reuse the existing `OwnerSelect` (the workspace's non-native owner picker); the **assignee filter** became the new `Select` (flattened its `<optgroup>`).

Result: **zero native `<select>`/`<option>` in app code** (only doc-comment mentions remain). Verified initial values still resolve to a shown label (severity→Medium, filters→"All severities"/"Everyone").

Verified: `tsc --noEmit` ✓ + `vite build` ✓.

**Still pending:** the **Projects → Testing** rename (label + routes) — paused mid-inventory when this came in.

---

## Prompt
> Change "Projects" to "Testing", and change all the routes.

Resumed the paused rename. Owner picked **"Label + URLs"** scope: rename the section label + frontend routes, **leave backend API paths and code/folders alone**.

## Work done
- **Routes `/projects` → `/testing`** across the router: `App.tsx` (`/testing` + `/testing/:projectId` workspace), `lib/nav.ts` path, and every internal link/`navigate()` — ProjectLayout, ProjectCard, FeatureSidebar, FeatureSummary, ReportView, DashboardPage, and the BugsBoard/Roadmaps/BugDetail back-links. Carefully **excluded** the 25 backend `apiGet('/projects…')` calls and the 3 `/public/projects/:token` share URLs (those are API/public, not routes).
- **Label "Projects" → "Testing"**: `nav.projects` (drives the sidebar item, ProjectsPage title, and back-link fallbacks) + the dashboard `home.statProjects` stat card.
- **Deliberately kept** entity-level copy ("New project", "Recent projects", "No projects yet") and all `features/projects/` folders/identifiers/API — renaming those reads awkwardly and was out of the chosen scope. Old `/projects` bookmarks fall through the existing `*` catch-all to `/` (no redirect, per the chosen option).

Verified: no frontend router `/projects` left (only file-path imports + API paths remain); `tsc --noEmit` ✓ + `vite build` ✓.

---

## Prompt
> sử dụng product discovery để write feature cần thiết cho `task.md` (`/features/task.md`) — mục đích là nơi Engineer write task and link tới backlog item.

Ran the **product-discovery** skill grounded in the actual codebase rather than a blank brainstorm. Key finding: the repo's own glossary + Spec 04 define **"backlog item" = "roadmap item"** verbatim, and there is **no `task`/`backlog` entity yet** — both are net-new. Mapped the domain: a roadmap item carries the *what/why* (RICE, a **manual** 0–100 `progress` slider) but there's **no engineering "how" layer**. Two adjacent surfaces are the wrong shape — **Bugs** are reactive, and the OKR **Individual** is stubbed (`IndividualStatus` = todo/in-progress/done is declared but **used nowhere**). **Bug is the precedent**: a top-level entity that flat-links to its source (`caseId`/`caseLabel`/`reportId`); a **Task** is the symmetric missing piece that flat-links to a **backlog item**.

## Work done — wrote `features/task.md` (PRD, ~matches `test-case-selections.md` style)
Proposed a **`Task`** = engineering execution on a backlog item. Discovery lens included (riskiest assumption **A1: will engineers actually record tasks here** → v1 must be one-line-to-add + drive the progress the PM already watches + show in a "My Tasks" queue).
- **Data model** — new top-level `Task` collection (flat fields per project rule), `status` **reuses `IndividualStatus`**, links via `roadmapItemId` + denormalized `roadmapItemLabel` + `roadmapId` (mirrors `Bug.caseLabel`). Rollup: item `progress = doneTasks ÷ totalTasks`.
- **v1 scope** — Task entity, link-to-backlog-item, Tasks panel inside `RoadmapItemDialog` (inline add), "My Tasks" view + Inbox `ASSIGNED_TASK`, progress rollup with manual override. Deferred: priority/estimate, activity thread, sub-tasks/deps, unlinked tasks, board view.
- **Build notes** — new `backend/.../tasks/` module templated on **bugs**; `frontend/src/features/tasks/`; reuse shared `Select` (non-native), assignee people-picker, i18n; no new brand colours.
- **Open decisions for owner** — backlog-item = roadmap item (confirm); own collection vs. embedded (rec: collection); computed-vs-manual progress (rec: computed + override); Task vs. OKR Individual (rec: separate, shared enum); enum rename (defer).

No code written — spec only, awaiting owner approval before build (per CLAUDE.md phases).

---

## Prompt
> (owner confirmed via discovery checkpoint) backlog item = **roadmap item**; storage = **own top-level collection**; **build v1 now**.

Built the **Tasks** feature v1 end-to-end (staged, each stage typechecked). Bug is the structural precedent throughout — a top-level entity that flat-links to its source.

## Backend — new `Task` module (3-layer, templated on bugs)
- **`application/tasks/`** — `TaskStatus` enum (todo/in-progress/done), `TaskEntity`/props, DTOs (create/update/update-status/query/response), `TaskMapper`, `ITaskRepository` port, 6 use-cases (create/get/list/update/set-status/delete), `ApplicationTasksModule`. Create/update resolve assignee name via the users repo (no notifier — webhooks skipped for v1).
- **`infrastructure/tasks/`** — `TaskSchema` (mongoose, `roadmapItemId`/`roadmapId`/`assigneeId`/`projectId` indexed), `TaskRepository` on `BaseRepository`, `InfrastructureTasksModule`.
- **`presentation/tasks/`** — `TasksController` (list/create/get/patch/patch-status/delete). **Permissions:** create/edit/status/**delete** = ADMIN+TESTER (delete is admin-only for bugs, but engineers own their tasks); guests read-only.
- **Wired** `/v1/tasks` into `presentation.module.ts` (import + RouterModule path). Link fields mirror `Bug.caseId/caseLabel/reportId` → `roadmapItemId/roadmapItemLabel/roadmapId`. Flat response per project rule. `tsc --noEmit -p tsconfig.build.json` ✓.

## Frontend
- **Enums** — repurposed the orphan `IndividualStatus` → canonical **`TaskStatus`** (zero external refs; identical values) + `TASK_STATUSES`/`_LABEL`/`_COLOR` (semantic tokens `--muted-foreground`/`--info`/`--success` — **no new brand colours**).
- **`types/dto.ts`** — flat `TaskDto`. **`features/tasks/api.ts`** — react-query hooks (`useTasks`/create/update/set-status/delete), same query pattern as bugs.
- **`TaskPanel`** (in `RoadmapItemDialog`, edit-mode only, rendered outside the item `<form>` so Enter never submits it) — inline add (title→Enter), non-native `Select` status pill, `ProgressBar` "N of M done" rollup, delete. **Assignee:** admins get the users `Combobox`; testers get an **"Assign to me"** toggle (self-assign works because the backend resolves assignee by id, not the admin-only users list). Passes `roadmapId`/`projectId` from `RoadmapBoardPage`.
- **`MyTasksPage`** (`/tasks`) — tasks assigned to me, grouped by status, each row links to its backlog item's roadmap. New **`nav.tasks`** item + `tasks` check-square icon in the "You" group. i18n keys added.

## Deferred (noted in features/task.md §5 "Later")
Inbox `ASSIGNED_TASK`, driving the actual `RoadmapItem.progress` write (v1 shows a computed rollup; manual slider untouched), `shortId` (bugs have none), due-date UI, priority/estimate, sub-tasks, cascade-delete of tasks when a roadmap item is removed.

Verified: backend `tsc` ✓, frontend `tsc --noEmit` ✓ + `vite build` ✓ (2084 modules). Not yet driven live (needs Mongo + auth) — offered to run the dev stack next.

---

## Prompt
> In a Testing detail page, move the Assignee widget to the right of the "Test Case Summary" heading — on the same row as the heading.

In `ReportSections.tsx` (the `Testing` section), the Assignee filter lived in a `.report-filterbar` up near the section title. Moved it down into the **"Test Case Summary" heading row**, right-aligned.

## Work done
- **`ReportSections.tsx`** — removed the top `.report-filterbar` block; rebuilt the heading as a flex row `.tc-summary-head` = `<h3>Test Case Summary</h3>` on the left + a right-side `.tc-summary-tools` group holding the **Assignee** filter (only when there are cases), the "X of Y cases" count, and the existing **↥ Import** button. Same state (`assignee`/`shownCases`), so filtering still drives the table.
- **`report-workspace.css`** — added `.tc-summary-head` (flex, space-between, wraps on mobile; `h3` margin reset) + `.tc-summary-tools`; added `.tc-summary-tools` to the `@media print` hide list so the filter doesn't appear in PDF/print exports (as the old filterbar did).

Verified: `tsc --noEmit` ✓ + `vite build` ✓. Filter wiring intact (table maps `shownCases`).

---

## Prompt
> thêm tính năng Tasks vào product. Phần này dành cho engineer, rạch ròi lại sidemenu giữa Product Discovery and product delivery.

Tasks was already built; this reorganized the sidebar to make the **Product Discovery vs Product Delivery** split explicit, and placed Tasks on the engineer/delivery side.

## Work done — sidebar IA (`lib/nav.ts` + `i18n/en.ts`)
- Renamed the two middle nav groups and reordered them lifecycle-first (discover → deliver):
  - **Product Discovery** (`navgroup.discovery`, was `planning`) — **Roadmaps · Milestones** (the *what & why*).
  - **Product Delivery** (`navgroup.delivery`) — **Testing · Bugs · My Tasks** (the *how*; engineer-facing).
- **Moved "My Tasks" out of "You"** into Product Delivery (kept **Inbox** in "You"). Overview/Admin unchanged.
- i18n: removed the now-dead `navgroup.planning` key, added `navgroup.discovery`; relabeled `navgroup.delivery` → "Product Delivery". Sidebar renders `t(group.headingKey)`, so headings update automatically.

Grounding: matches the product overview's own framing — roadmap/OKR = plan the *what/why* (discovery), test-cases/bugs/tasks = build & verify the *how* (delivery).

Verified: `tsc --noEmit` ✓ (no stale `navgroup.planning` refs) + `vite build` ✓ (2.38s). No colour changes. `features/task.md` §7 updated to note My Tasks now lives under Product Delivery.

---

## Prompt
> User can create task from /tasks.

`/tasks` (My Tasks) was read-only — tasks could only be created from a roadmap item's panel. Added direct creation. Backend already allows an empty link, so this is **frontend-only**.

## Work done
- **`features/tasks/components/CreateTaskDialog.tsx`** — new create dialog (mirrors `CreateBugDialog`): **Title** (required) · **Backlog item** (optional `Combobox` over *every* roadmap item, "No backlog item" first) · **Description**. Picking an item back-fills the same flat link the panel writes (`roadmapItemId` + canonical `roadmapItemLabel` + `roadmapId` + `projectId`, resolved from `useRoadmaps()`). **Auto-assigns to the current user** (`assigneeId: user.id`) so it lands in their list — with an "assigned to you" hint. Inline error banner; resets + closes on success (react-query invalidates `['tasks']` → list refreshes).
- **`MyTasksPage.tsx`** — **"New task"** button in the `PageHeader` actions and in the empty state (both writer-only: admin/tester). Renders the dialog.
- **i18n** — `tasks.new`/`titleLabel`/`descriptionLabel`/`noBacklogItem`/`assignedToYouHint`.
- Delivered the §5 "Later" item **unlinked personal tasks** (link is now optional at creation).

Verified: `tsc --noEmit` ✓ + `vite build` ✓ (2.46s). No new colours (reuses Dialog/Combobox/Button on existing tokens). Guests see no create affordance.

---

## Prompt
> `const isAdmin = user?.role === Role.ADMIN;` is duplicated — use a shared function.

`user?.role === Role.ADMIN` (×10) and `… === Role.ADMIN || … === Role.TESTER` (×11) were re-derived in nearly every page. Centralized them.

## Work done — single source of truth in `lib/auth.tsx`
- `useAuth()` now returns derived **`isAdmin`** and **`canWrite`** (= admin \|\| tester) alongside `user` — computed once in the `AuthProvider`.
- Refactored **14 files** to destructure `{ isAdmin, canWrite }` from `useAuth()` and deleted the local re-derivations: Sidebar, ProjectsPage, ProjectLayout, ReportView, BugsBoardPage, BugDetailPage, RoadmapsPage, RoadmapBoardPage, MilestonesPage, MilestoneDetailPage, AdminPeoplePage, AdminSettingsPage, MyTasksPage, TaskPanel. Pruned the 13 now-unused `Role` imports (`.tsx` isn't covered by the `.ts`-only lint script, so removed by hand). `AdminPeoplePage` keeps `Role` (form default).
- Only remaining role comparison is in `lib/auth.tsx`.

⚠️ **Flagged, not changed:** the backend just added **`Role.PRODUCT`** to task `@Roles`, but the frontend `Role` enum has no `PRODUCT` and `canWrite` = admin\|\|tester. A product-role user would be **read-only in the UI**. Now that the gate is centralized, granting them write access is a one-line change in `auth.tsx` (+ adding `PRODUCT` to the frontend `Role` enum) — awaiting a decision.

Verified: `tsc --noEmit` ✓ + `vite build` ✓ (2.54s). Behaviour unchanged (same admin/tester semantics).

---

## Prompt
> Modals should appear centered — no "fly in from bottom-right".

Root cause: the dialog centered via `left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2`, but `tailwindcss-animate`'s `animate-in`/`zoom` overrides `transform` during the animation, so it interpolated the translate from `(0,0)`→`(-50%,-50%)` — a diagonal fly. Fixed both `components/ui/Dialog.tsx` and `features/reports/components/CaseEditDialog.tsx` by centering **without transform** (`inset-0 m-auto h-fit`), so the fade/zoom now happens in place. Verified `tsc` + `vite build`.

## Prompt
> Milestones → OKRs.

Renamed routes `/milestones` → `/okrs` (App.tsx, nav.ts, MilestonesPage, MilestoneDetailPage, DashboardPage stat) and user-facing labels (nav, stat, page title, and entity copy — "New OKR", "No OKRs yet", "OKR not found", "Delete OKR", since the feature already models objectives + key results, so "OKR" reads naturally). Kept API paths, `features/milestones` folder, and identifiers. Verified `tsc` + `vite build`.

## Prompt
> Add a **Product** role: full permission on Delivery, and edit Roadmap & OKR names. → (confirmed) full incl. project mgmt; full edit of Roadmaps/OKRs except delete.
> Add a **Developer** role: just maintain Delivery.

Two new roles across backend + frontend. The `RolesGuard` has no superuser shortcut, so each role was added to every endpoint it should reach.

**Permission matrix (backend `@Roles` + frontend `useAuth()` gates):**
| Capability | Admin | Tester | Product | Developer |
|---|---|---|---|---|
| Edit Delivery (test cases, bugs, tasks) | ✓ | ✓ | ✓ | ✓ |
| Manage Delivery (archive/delete projects, delete bugs, assign) | ✓ | — | ✓ | — |
| Create/edit Roadmaps & OKRs | ✓ | ✓ | ✓ | — |
| Delete Roadmaps/OKRs | ✓ | — | — | — |
| People & Settings | ✓ | — | — | — |

## Work done
- **Backend** — `libs/core/interfaces/role.enum.ts`: added `PRODUCT='product'` + `DEVELOPER='developer'`. Added `Role.PRODUCT` across Delivery controllers (projects, reports, groups, bugs, tasks, activity — every `@Roles`) and to roadmap/OKR create/edit (not delete); added `Role.DEVELOPER` to the delivery-content controllers (reports, bugs, tasks, activity — the `admin,tester,product` endpoints only). Added Product to users **GET** (read the people list, for assignees) and to comment moderation overrides. `nest build` ✓.
- **Frontend** — `types/enums.ts`: added both roles + `ROLE_LABEL` (auto-appears in the People role picker). Centralized the gates in `lib/auth.tsx` (aligning with the in-flight refactor): `canWrite` [admin,tester,product], `canEditDelivery` [+developer], `canManageDelivery` [admin,product], `isAdmin` [admin]. Delivery pages alias the new gates onto their `useAuth()` destructure (`canEditDelivery: canWrite`, `canManageDelivery: isAdmin`); Discovery/admin pages keep `canWrite`/`isAdmin` (now correct for Product/Developer automatically). `tsc` + `vite build` ✓.

---

## Prompt
> Admin: full permission on editing features.

Admin already had full feature editing (it passes both the `canEditDelivery` and `canManageDelivery` gates). While verifying, surfaced an inconsistency: features could be **added** by anyone who edits Delivery, but only admin/product could **rename/delete** them or manage groups. Owner chose to align it → feature add/rename/delete + group management all become **`canEditDelivery`** (admin, tester, product, developer).

## Work done
- **Backend** — `reports DELETE` (delete feature) and all **groups** endpoints (create/reorder/update/delete): `[admin, product]` → `[admin, tester, product, developer]`. `nest build` ✓.
- **Frontend** — `FeatureSidebar`: dropped the `isAdmin` prop and gated all management (rename/delete feature, drag-reorder, add/rename/remove group) on `canWrite`, which `ProjectLayout` feeds from `canEditDelivery`; removed the `isAdmin={…}` pass-through (ProjectLayout keeps `isAdmin`/`canManageDelivery` for the archive-project action). `tsc` + `vite build` ✓.

Net: admin still fully edits features (unchanged); tester & developer can now rename/delete features and manage groups, matching their add-feature access.

---

## Prompt
> `/testing/:projectId` — redesign header breadcrumb.

The project-workspace topbar led with a `← Testing` back-link + the project title as a bare `<h1>`. Replaced it with a proper **breadcrumb**.

## Work done (`ProjectLayout.tsx` + `report-workspace.css`)
- Markup: `<nav class="breadcrumb" aria-label="Breadcrumb">` → **`Testing`** (link to `/testing`, muted) · **`ChevronRight`** separator (lucide, `size={14}`) · **`{project.title}`** as the current crumb (still the page `<h1>`). Env badge unchanged. Dropped the `ArrowLeft` back-link (the "Testing" crumb is the up-nav now) → swapped the import to `ChevronRight`.
- CSS: removed `.brand-up`; added `.breadcrumb` (flex, `min-width:0`), `.breadcrumb-sep` (`--faint`), `.breadcrumb-current` (`min-width:0` so the title ellipsizes). **Scoped the link as `.topbar .breadcrumb-link`** (specificity 0,2,0) so its muted colour beats `.report-workspace a { color: inherit }` (0,1,1) — otherwise the parent crumb rendered full-strength (a latent bug in the old `.brand-up`). Hierarchy now reads right: muted parent → faint chevron → emphasized current title.
- Tokens only (`--muted`/`--text`/`--faint`) — no new colours; truncates + wraps within the existing responsive topbar.

Verified: `tsc --noEmit` ✓ + `vite build` ✓ (2.58s).

---

## Prompt
> Testing detail — add an X (close) button that returns to the previous page.

Added a square **X close button** to the far-right of the project-workspace topbar (`ProjectLayout`), wired to `navigate(-1)` (back to the previous page). Styled `.topbar-close` to match the `⋯` menu trigger (28px ghost icon button); new i18n `common.close`. Hidden in print with the rest of `.topbar`. `tsc` + `vite build` ✓.

---

## Prompt
> Redesign the roadmap item modal to the two-column layout in [screenshot]. (Chose "Full build" — add the 3 missing fields.)

The mockup adds **UI-reference image, start date, assignees** — none in the roadmap-item model. Owner picked full build.

## Backend — 3 new item fields (near-zero plumbing)
Roadmap items are stored as `[Schema.Types.Mixed]`, the replace-items DTO is bare `@IsArray()`, `replaceItems()` stores the array as-is, and the mapper spreads `...item` — so new fields round-trip **without** schema/DTO/validation changes. Only touched types:
- `roadmap-item.type.ts` — added `imageUrl`, `startDate`, and `assignees: RoadmapAssignee[]` (`{id,name}` denormalized so names show without the admin-only user list). `roadmap.response.dto.ts` — mirrored + `RoadmapItemAssigneeDto`. `roadmap.mapper.ts` — defensive defaults (`?? ''` / `?? []`) so pre-existing items return a clean typed shape. Backend `tsc` ✓.

## Frontend — two-column `RoadmapItemDialog` (`dto.ts` + `en.ts` + the dialog)
Widened the `Dialog` to `max-w-5xl`; `grid lg:grid-cols-[minmax(0,1fr)_340px]` (stacks on mobile).
- **Left:** large title input + tall `Description` textarea (`min-h-[300px]`, grows) + the existing **TaskPanel** (kept, edit-mode).
- **Right sidebar:** **UI reference** (preview / `No image` placeholder + **paste-URL** input + Remove — no upload endpoint exists, so URL-based for now); **Start date** (our `DatePicker`, not native); **Assignees** (denormalized chips w/ avatar-initial + remove for writers; add-picker `Select` gated on `canManageDelivery` since it needs the user list); boxed **RICE** card (reach/impact/confidence/effort + live **Score** to 1 dp in `--primary`); **Progress** range slider + number + %; **Phase / Status / Difficulty** selects.
- Footer = single **Done** (submits; X/Esc/overlay cancels), matching the mockup.
- **Kept existing RICE math/scale** (didn't silently switch to the mockup's 1–5 scale). **Added Phase** (mockup omitted it, but it's the board column — kept for function). No new colours; non-native `Select`s.

Verified: backend `tsc` ✓, frontend `tsc --noEmit` ✓ + `vite build` ✓ (2.64s). Not driven live — offered to screenshot. Follow-ups: image file-upload (needs an endpoint), fullscreen-expand toggle (Dialog builds its own header).

---

## Prompt
> RICE max is 5, min is 1.

Put the four RICE factors (Reach, Impact, Confidence, Effort) on a uniform **1–5** scale.

## Work done (`RoadmapItemDialog` + i18n)
- RICE inputs now `min={1} max={5}`, with a `clampRice` guard clamping typed values to 1–5.
- New-item defaults changed from mixed scales (reach 100, confidence 80) to a neutral **3** each.
- Labels/help updated: "Confidence %" → "Confidence"; help text now reads "rate 1 (…) to 5 (…)".
- The RICE **score** stays the derived priority `(R×I×C)/E` (unchanged); the chart auto-scales to the new range.

Frontend-only (backend `riceScore` computes from stored values). `tsc` + `vite build` ✓. Note: existing items keep their old numbers until re-rated.

---

## Prompt
> Roadmap item — remove the "UI reference" selection.

Removed the **UI reference image** section from `RoadmapItemDialog` (the preview + "paste image URL" input + remove button in the right sidebar). Cleaned up the now-unused `Trash2` import and the four `roadmaps.uiReference/noImage/pasteImageUrl/removeImage` i18n strings. Kept the `imageUrl` data field (defaults to `''`) so nothing else has to change. `tsc` + `vite build` ✓.
