# 2026-09-18

## Prompt

> làm giúp 1 cái collapse để đóng mấy cái backlog item trên timeline lại, có nút collapse all và expand all
> (ảnh: Roadmaps → [Launching] Linkadz v4 → tab Timeline)

## Đã làm

Thêm khả năng **gập/mở** cho timeline: mỗi backlog item gập được đám task con của nó, kèm cặp nút
**Collapse all / Expand all** ở hàng legend phía trên chart.

- **`components/GanttChart.tsx`** (component dùng chung cho mọi timeline):
  - prop mới `collapsible`. Cây cha–con **suy ra từ `depth`** có sẵn — row `depth: 0` sở hữu mọi row
    thụt vào ngay sau nó, cho tới row `depth: 0` kế tiếp hoặc một group header (sprint). Caller không
    phải bịa thêm `parentId` nào.
  - state `collapsed: Set<rowId>` — mặc định mở hết, đúng như hành vi cũ.
  - chevron nằm **cạnh** cell (không lồng trong), vì cell vốn đã là `<Link>`/`<button>`. Row không có
    con vẫn giữ chỗ trống `w-6` để mọi title thẳng hàng; padding của cell tự trừ lại phần gutter đó.
  - Collapse all / Expand all đặt cuối hàng legend (`ml-auto`), tự disable khi đã ở trạng thái đó.
    Không có row nào lồng nhau → không hiện nút, không hiện chevron.
  - **Khung ngày (axis) vẫn tính trên toàn bộ rows**, kể cả row đang gập — gập một item không làm
    trục thời gian nhảy dưới chân mấy cái bar còn lại.
- **`features/roadmaps/components/RoadmapGanttView.tsx`**: truyền `collapsible` vào `GanttChart`.
- **`i18n/en.ts` + `i18n/ko.ts`**: `boards.collapseAll`, `boards.expandAll`, `boards.collapseRow`,
  `boards.expandRow`.

## Không đụng tới

- **`features/issues/IssueTimelineView.tsx`** (Bugs / My Tasks / All issues): timeline ở đó phẳng,
  không có row `depth > 0`, nên không bật `collapsible` — mà có bật cũng tự tắt vì không có con nào.
- Public share page render `RoadmapGantt` trực tiếp → cũng được gập luôn, vì `collapsible` nằm trong
  `RoadmapGantt` chứ không phải bản authenticated.

`tsc --noEmit` sạch (chỉ còn lỗi cũ do chưa cài `vitest`/`@types/node`). **Chưa thử tay trên browser.**

---

## Prompt

> sửa giúp cái upload hoạt động giống portal-v2 đi, dùng presigned-url
> hiện tại upload bằng http request tới server vừa ngốn băng thông, lâu mà còn rủi ro đứt file
> giữa chùng, presigned-url ko có đúng ko

## Đã làm

Upload giờ đi **thẳng browser → S3**. API chỉ ký một URL dùng một lần, không còn nhận bytes.

- **`POST /v1/uploads/presign`** (mới) — nhận `{ name, contentType, size }`, trả về
  `{ uploadUrl, url, name, contentType, size, headers, expiresInSeconds }`. TTL 1 giờ.
- **`use-cases/upload-policy.ts`** (mới) — `resolveUpload()`: chỗ **duy nhất** phán xét một file
  (storage đã cấu hình chưa · type có được nhận không · có quá cap của tenant không). Cả hai cửa
  (presign và multipart cũ) đều gọi nó, nên luật không thể lệch nhau. Khác biệt: presign từ chối
  **trước khi** gửi byte nào, thay vì sau khi đã upload xong cả file.
- **`StorageService.createUploadUrl()`** — ký `ContentLength` **và** `ContentType`
  (`signableHeaders`). Đây không phải chi tiết thừa:
  - không ký `ContentLength` → client khai gian size là lách được cap của tenant;
  - presigner **mặc định không ký `content-type`** → object lưu theo type client gửi, tức
    `spec.pdf` up dưới dạng `text/html` sẽ được serve như một trang web trên domain storage —
    đúng cái `classifyUpload` sinh ra để chặn. Đã kiểm chứng cả hai đều bị S3 trả **403**.
- **Bucket + CORS** được chuẩn bị *trước* khi ký (URL đã ký thì không retry được nữa), cache một
  lần mỗi process. CORS là best-effort: MinIO không implement `PutBucketCors`, key hẹp quyền cũng
  không có `s3:PutBucketCors` → log warning rồi đi tiếp thay vì chặn upload.
- **`features/uploads/api.ts`** — `uploadMedia()` giữ nguyên chữ ký, mọi call site không đổi.
  Bên trong: presign → `XMLHttpRequest` PUT (XHR vì chỉ nó báo progress *upload*; và **không** dùng
  axios instance của app, vì nó gắn bearer token vào mọi request — thêm `Authorization` vào một
  presigned PUT là S3 đọc nó thay chữ ký, hỏng mọi upload).
- **Fallback** về route multipart cũ khi: API chưa có route (404, lúc rolling deploy), CORS chặn,
  hoặc provider không ký được. Người dùng không thấy gì khác ngoài việc nó nhanh hơn.
- **`lib/api.ts`** — `ApiError` mang thêm `status`, để phân biệt "route không tồn tại" với
  "file bị từ chối".

## Azure: cố ý KHÔNG đổi

Blob SAS ký **URL**, không ký được request header → không ghim được content-type của object, tức là
mất đúng cái bảo đảm ở trên. Cộng thêm việc không có tài khoản Azure để test thật. Nên Azure giữ
nguyên đường cũ (bytes qua API), `createUploadUrl` trả `null` và frontend tự fallback.

## Kiểm chứng

`storage.service.integration.spec.ts` chạy với MinIO thật (opt-in qua `S3_TEST_ENDPOINT`, không có
thì skip): PUT đúng → 200 và object lưu đúng type; khai gian size → 403; đổi content-type → 403.
Full suite backend: 745 pass / 4 skip. `tsc --noEmit` sạch cả hai bên.

**Chưa thử tay trên browser thật** — cần một bucket có CORS cho phép PUT từ origin của app.

---

## Prompt 3 — "upload mất mạng giữa chừng có retry được ko ta, cái presigned-url này nè"

Lúc hỏi thì **chưa có retry**, và tệ hơn: đường fallback vừa làm xong lại xử lý sai đúng ca này —
rớt mạng giữa chừng sẽ rơi vào nhánh "chắc là CORS", in một warning sai sự thật rồi up lại toàn bộ
file qua đường API chậm hơn (và nhiều khả năng cũng rớt tiếp).

**Vấn đề:** trình duyệt báo *preflight bị CORS chặn* và *mất kết nối giữa chừng* **giống hệt nhau**
— status 0, event `error`, không kèm thông tin gì (cố ý, để một trang không thể dò origin khác).

**Cách phân biệt:** có byte nào đã đi chưa. Có byte đi nghĩa là preflight đã qua, URL và CORS đều ổn
→ lỗi mạng → retry lại chính URL đó. Không byte nào đi → chưa vào được cửa → fallback qua API.
Cờ `sentAnyBytes` set trong `xhr.upload.onprogress`, gắn vào `DirectUploadError.retryable`.

- `putWithRetry()` — tối đa **3 lần**, nghỉ giãn dần 1s/2s, `onProgress(0)` giữa các lần (thanh
  tiến trình phải tụt về 0, vì lần sau thật sự bắt đầu lại từ byte đầu).
- Retry lại **cùng URL đã ký** là an toàn và miễn phí: URL sống 1 giờ, và PUT cùng key là *ghi đè* —
  phần ghi dở của lần trước bị thay, không bị nối thêm.
- Lỗi có phản hồi (403 chữ ký hết hạn, header không khớp) → `retryable: false`: gửi lại y hệt thì
  nhận lại y hệt, cho đi đường API luôn.
- Hết 3 lần thì báo lỗi thật cho người dùng, không fallback — mạng đang hỏng, đường API cũng hỏng.
  Message viết thành câu hoàn chỉnh vì `useUploadQueue` hiển thị thẳng nó lên danh sách upload.

**Giới hạn — nói thẳng:** đây là *retry*, không phải *resume*. Một PUT không biết nó đã đi tới đâu,
nên 40MB đã lên coi như mất, lần sau chạy lại từ đầu. Muốn resume thật thì phải dùng **S3 multipart**
(chia file thành part, ký và xác nhận từng part, rớt thì chỉ mất part đang bay). Thay đổi lớn hơn
nhiều và chỉ đáng tiền ở kích thước file mà cap hiện tại của sản phẩm chưa cho phép (video 30MB,
doc 25MB, ảnh 10MB) → **để anh quyết**, chưa làm.

---

## Prompt 4 — "làm trong này luôn được không, nếu được làm luôn"

Chốt làm **S3 multipart** — tức resume thật, không phải retry lại từ đầu.

### Ý chính

File > 10MB được ký thành **nhiều part 5MiB**, mỗi part một URL ký riêng. Part nào rớt thì gửi lại
**mình part đó**; các part đã xong vẫn còn nguyên trên bucket. Rớt mạng ở 90% giờ mất 5MB, không
mất cả file.

5MiB không phải con số tuỳ ý — đó là **mức tối thiểu của S3** cho mọi part trừ part cuối; nhỏ hơn
thì `CompleteMultipartUpload` fail ở đúng bước cuối, sau khi đã up xong cả file.

### Một cửa, API quyết định đường đi

`POST /uploads/presign` vẫn là cửa duy nhất, câu trả lời nói rõ file này đi đường nào:
`uploadId` có → đi theo part; `uploadUrl` có → một PUT; không cái nào → vòng qua API (Azure).
Client đọc chứ không tự đoán theo size. Thêm 2 route: `multipart/complete`, `multipart/abort`.

### Bảo mật không hở thêm chỗ nào

- **Content-type** được đặt ở `CreateMultipartUpload`, do server đặt, các part **không mang** header
  này → không thể bị đổi. Chặt hơn đường một-PUT (ở đó phải cố ý thêm `signableHeaders`).
- **Size** ký `ContentLength` chính xác cho từng part (part cuối là phần dư) → padding một part bị
  **403**, cap của tenant không lách được khi chia nhỏ.
- **Key** client gửi lên ở `complete`/`abort` được validate theo đúng shape `buildKey` sinh ra, và
  bucket luôn đọc từ settings của tenant gọi API — không bao giờ từ request.
- `complete` từ chối etag rỗng: ghép thiếu part sẽ lưu một file thủng lỗ mà vẫn báo thành công.

### Dọn rác

Tab đóng giữa chừng để lại part trên bucket — không hiện trong list file nhưng **vẫn tính tiền
lưu trữ mãi mãi**. Hai lớp: browser gọi `abort` khi bỏ cuộc, và bucket có lifecycle rule
`AbortIncompleteMultipartUpload` (7 ngày), set best-effort giống CORS.
Rule này **cố ý không có `Expiration`** — nó chỉ xoá part dở, không bao giờ xoá file đã lưu.
MinIO từ chối rule chỉ-có-abort (đã probe cả 4 shape) nên log warning rồi đi tiếp; vô hại vì MinIO
tự dọn stale upload sau 24h. Chỉ AWS mới thật sự cần rule này.

### Frontend

- 3 part song song. Lỗi cứng của một part **huỷ luôn các part còn lại** (AbortController nội bộ
  nối với signal của caller) thay vì để cả ba cùng đâm vào một bức tường.
- ETag là thứ để ghép part — bucket thiếu `ExposeHeaders: ETag` thì part up được nhưng không bao giờ
  ghép được. Bắt riêng ca này và vòng qua API, message nói rõ nguyên nhân.
- Progress chỉ chạm **100 sau khi ghép xong**, không phải sau part cuối — vì ghép vẫn có thể fail.

### Kiểm chứng

- MinIO thật, 7 test pass: ghép 3 part thành đúng 1 object đúng size đúng type; part quá cỡ → 403;
  abort xong thì không complete được nữa.
- 8 test frontend giả lập XHR: part rớt giữa chừng chỉ gửi lại part đó (part 1 đi 1 lần, part 2 đi
  2 lần); 3 lần rồi mới chịu thua; CORS chặn thì không retry; thiếu ETag thì vòng qua API; hủy thì
  dừng hẳn; progress chạm 100 đúng lúc.
- Backend 754 pass / 7 skip. Frontend 152 pass. `tsc --noEmit` **sạch hoàn toàn cả hai bên**
  (các lỗi spec file trước đây là do thiếu devDependencies, `npm install` xong là hết).

**Vẫn chưa thử tay trên browser thật** — cần bucket production có CORS cho phép PUT **và**
`ExposeHeaders: ETag` từ origin của app.

---

## Prompt 5 — "multipart kiểu này request có đi qua server ko"

Không. Với file 30MB: **2 request JSON nhỏ** đi qua server (`/uploads/presign` để ký, và
`/uploads/multipart/complete` để ghép) — mỗi cái vài trăm byte. **6 PUT chứa byte thật đi thẳng
lên S3**, server không nhìn thấy một byte nào. Sở dĩ vẫn còn 2 request đó là vì server giữ secret
key của bucket — nó ký hộ, chứ không chở hộ.

---

## Prompt 6 — "rồi làm cái đó sao" (set CORS cho bucket production)

### Lỗi tự bắt được trước khi trả lời

`ensureBucketCors` đang coi bucket là "đã cấu hình rồi" khi thấy **bất kỳ** rule nào cho `PUT`.
Một bucket có rule PUT nhưng thiếu `ExposeHeaders: ETag` sẽ nhận hết part rồi **không bao giờ ghép
được** — mà app lại bỏ qua, không sửa, không log. Tenant đó kẹt đường chậm vĩnh viễn.

Sửa: tách ra `allowsBrowserUpload(rule)` — phải có **cả hai** (PUT *và* ETag/`*` trong
`ExposeHeaders`) mới tính là đủ. Export ra + 6 unit test (`storage.service.spec.ts`), vì đây là
hàm âm thầm quyết định một workspace được đi đường nhanh hay không.

### Script kiểm tra + sửa bucket

`backend/scripts/check-storage-cors.ts`:

```bash
npm run storage:cors                                      # DRY RUN — chỉ báo cáo
npm run storage:cors -- --apply --origin=https://app-cua-anh.com   # thêm rule còn thiếu
```

Đọc `CloudStorageConfig` của từng workspace trong Mongo, mở bucket đúng key của workspace đó, in ra
từng CORS rule hiện có (✅ vào cái đạt), và nói thẳng ca nào đang hỏng: *"PUT được nhưng không lộ
ETag"* khác hẳn *"không rule nào cho browser PUT"*. `--apply` thì **thêm** rule (không bao giờ thay
rule cũ — bucket có thể đang dùng chung). Import thẳng `allowsBrowserUpload` / `BROWSER_UPLOAD_CORS`
/ `ABANDONED_PARTS_RULE` từ service, nên script không thể phán khác với đường upload thật.

Script cũng tự dò tên collection thay vì hardcode `appsettings` — đoán sai thì nó sẽ báo "không có
workspace nào", đọc y như "mọi thứ ổn", đúng cái câu trả lời sai duy nhất không được phép có.

### Kiểm chứng

Backend `tsc --noEmit` sạch, **760 test pass / 7 skip**.

---

## Prompt 7 — 5 việc từ Sếp + Evelyn

> Sếp cần:
> Ở tab timeline, bổ sung thêm View Mode xem dưới dạng Calendar
> Bổ sung thêm filter ngày tháng, assignee để xem trong khoảng thời gian đó đã làm được những việc gì
>
> Evelyn cần:
> Tính năng tự động update % task dựa theo status của Sub-task
> Task tự động chạy qua Done khi set status, không cần kéo sang thủ công
> Hide tính năng sprint đi để vào xem được board hiện tại luôn
>
> build luôn nhé

### 1. Calendar view

Tab **Calendar** đứng cạnh Timeline trên cả 4 board (All issues · My tasks · Bugs · public team board).

- **`components/CalendarView.tsx`** (mới) — surface dùng chung, anh em với `GanttChart`: không tự
  fetch, chỉ nhận `CalendarEvent[]`. Lưới tháng bắt đầu từ **thứ Hai**, bar xếp lane kiểu greedy
  (span dài xuống trước), ngày đọc bằng regex ra UTC nên **không bao giờ lệch múi giờ**.
- **Responsive:** dưới `md` lưới 7 cột không đọc được → đổi hẳn sang **agenda theo ngày**, chứ không
  phải thu nhỏ lưới lại.
- **`features/issues/IssueCalendarView.tsx`** (mới) — adapter, mirror `IssueTimelineView` từng field
  một (cùng `items`, cùng override lookup, cùng `IssuePeekDrawer`). Issue **không có ngày thì không
  hiện**: calendar là để đặt việc vào thời gian, việc chưa có ngày thì không có chỗ trên đó — nó vẫn
  nằm nguyên trên board/list/timeline.
- Tháng đang xem nằm trong URL (`?month=2026-09`, tháng hiện tại thì không ghi param) — mở một issue
  từ tháng 3 rồi bấm Back vẫn về tháng 3, không rơi về hôm nay.

### 2. Filter khoảng thời gian

Filter **assignee đã có sẵn** trên mọi board (khối `issueSharedFilters`) — phần thiếu là khoảng
thời gian *làm việc*, nên thêm axis **Scheduled**.

- Khớp theo **overlap**, không phải containment: issue mở từ tháng 6, đóng tháng 8 thì **có** làm
  trong tháng 7 — filter `createdAt`/`resolvedAt` đều không trả nó ra, `scheduled` thì có.
- Backend: `scheduledOverlapExpr()` trong issue repository — `$expr` ở **top level** (vì `$or`/`$and`
  đã có chủ: assignee và search), so sánh **chuỗi ngày** `YYYY-MM-DD` trực tiếp, không parse Date
  (chuỗi đó vốn đã sort đúng thứ tự thời gian). 9 unit test tự viết evaluator cho `$cond/$ne/$lte/$gte`
  để pin semantics mà không cần DB.
- Frontend **cố ý không** đẩy qua `dateRangeParams`: `createdAt`/`resolvedAt` là *timestamp* nên phải
  đổi ra instant của user; `startDate`/`endDate` là *ngày lịch* — "3 tháng 8" trên kế hoạch thì ở đâu
  cũng là 3 tháng 8. Ép thành instant là tự tạo lại đúng cái lệch ±1 ngày.

### 3. % tự động theo sub-task

**Tính lúc đọc, không lưu** → không bao giờ lệch.

- **`domain/issue-progress.ts`** (mới) — `progressOf()`: có sub-task thì % = tỉ lệ sub-task done;
  không có thì 0, hoặc 100 nếu chính nó đã done.
- `childRollups()` — một aggregation duy nhất cho cả trang (group theo `parentId`, đếm `done` theo
  **đúng vocabulary của kind từng dòng**: bug là resolved/closed, task là done).
- Response có thêm `progress` · `subtaskCount` · `subtaskDoneCount`.
- UI: `<SubtaskCount>` ("3/5") trong cụm chip của card + row list, và thanh progress trên card.
  **Leaf thì không hiện gì** — "0/5" trên mọi card là nhiễu, và thanh progress trên leaf chỉ nhắc
  lại đúng cái cột nó đang đứng.

### 4. Tự động qua Done

`SetIssueStatusUseCase` giờ **leo lên cha** sau mỗi lần đổi status:

- Con done hết → cha tự qua Done (bug: Resolved). Con bị mở lại → kéo cha ra khỏi Done về In progress.
- Cha đang ở **cột custom của team** thì **để yên** trên đường đi Done — chỉ kéo ra khi nó đang Done.
- Không cần đọc config team: built-in status key **không xoá được** (chỉ đổi tên/màu/thứ tự).
- Chặn vòng lặp bằng `Set` đã thăm + trần 5 tầng. Activity log giữ **đúng người thật** đã bấm, kèm
  cờ `automated: true` — không đổ cho SYSTEM.

### 5. Sprint

`SPRINT_DEFAULT` đổi từ `current` → `all`. Mở roadmap là thấy **nguyên board**, không rơi vào trong
một sprint nữa (và lúc cooldown thì không có sprint nào current cả — board trông như hỏng chứ không
như đang lọc). Banner sprint vẫn còn, chọn sprint vẫn ghi `?sprint=` nên link vẫn share được.

### Kiểm chứng

Backend `tsc --noEmit` sạch, 170 test pass (issues + saved-views, gồm 18 test mới).
Frontend `tsc --noEmit` sạch, 152 test pass, `npm run build` xanh.

**Chưa thử tay trên browser.**

---

## Prompt 8 — Nút đổi Gantt ↔ Calendar trong tab Timeline của roadmap

> oke, phần timeline sếp muốn có nút để đổi qua calendar thay vì chỉ view bằng gantt chart thôi á.
> calendar thì cho readonly thôi ko cho edit
>
> *(kèm ảnh: khoanh đỏ góc phải hàng legend của Gantt, chỗ "Collapse all / Expand all")*

### Nút nằm đúng chỗ khoanh đỏ

`GanttChart` có thêm slot `toolbar` — render ở **bên phải hàng legend**, ngay trước
Collapse/Expand all. `CalendarView` vốn đã có slot `toolbar` ở cùng vị trí (bên phải hàng
điều hướng tháng), nên cùng một nút được truyền vào cả hai view và **không bị nhảy chỗ** khi đổi qua lại.

Slot `toolbar` của Gantt **sống sót cả lúc loading và lúc rỗng** (legend thì không) — nếu không,
timeline trống sẽ nuốt mất nút và không còn đường quay lại.

### Calendar read-only

`RoadmapCalendarView` — bản đọc theo tháng của đúng những item mà Gantt vẽ:

- **Không có drag.** Gantt là nơi sửa ngày (kéo thanh, kéo mép). Một ô lịch rộng đúng 1 ngày,
  kéo thả sẽ làm tròn ngày đã set kỹ về ô mà con trỏ rơi vào.
- Click item vẫn **peek** đúng drawer mà Gantt mở — đó là *đọc*, không phải *sửa*.
- **Chỉ vẽ item, không vẽ task con.** Một cycle thường 15 item + 60 task; nhét chung vào một tháng
  là không đọc nổi. Đây chính là cách đọc "Collapse all" của Gantt — "item nào rơi vào lúc nào?".

### Hai view không được nói khác nhau

Tách khỏi `RoadmapGanttView` hai hàm dùng chung, cả hai view cùng gọi:

- `itemWindow(item, tasks)` — item chạy từ ngày nào tới ngày nào (start › startedAt › createdAt;
  end › completedAt › task cuối › +2 tuần). Nếu mỗi view tự tính, một item bắt đầu ngày 3 bên
  Gantt mà ngày 5 bên Calendar là loại bug chỉ lộ ra khi đổi view.
- `chartedItems(items, columns, narrowed)` — chỉ cột "Now", trừ khi board đã bị lọc sẵn theo sprint.

Màu cũng lấy từ **màu cột của chính item** như Gantt, không chế màu mới.

### State & chi tiết

- `?tl=calendar` trong URL (theo đúng nếp `?group=sprint`); `gantt` là mặc định nên không ghi vào URL.
  Link mở thẳng calendar share được.
- Nút "Group by sprint" **ẩn khi ở calendar** — gom nhóm là cách đọc của biểu đồ (xếp hàng dưới
  header sprint), lịch tháng không có hàng để xếp.
- Tháng đang xem đi qua `?month=` (dùng lại `useMonthParam` của các board issue).
- Nút dùng token sẵn có (`border` + `bg-card` + `secondary` cho bên đang bật) — **không thêm màu mới**.
- Responsive: dưới `sm` nút chỉ còn icon; dưới `md` lịch tự chuyển thành agenda theo ngày.
- i18n: `roadmaps.timelineMode/timelineGantt/timelineCalendar/calendarEmpty/calendarEmptyHint`
  (cả `en.ts` và `ko.ts`).

### Kiểm chứng

Frontend `tsc --noEmit` sạch, 152 test pass, `npm run build` xanh. **Chưa thử tay trên browser.**

### Khác biệt còn để ngỏ

Board issue (Bugs / My Tasks / Team board) có Calendar là **tab riêng** ngang hàng Timeline;
roadmap thì Calendar là **nút bên trong tab Timeline** (đúng chỗ sếp khoanh). Nếu muốn thống nhất
một kiểu cho cả hai thì nói, sửa nhanh.

---

## Prompt: Progress của backlog item phải tự tính, không kéo được

> phần auto-update progress request
> hiện tại khi đổi status task trong backlog item thì số % nó chưa update theo, cho số đó không kéo được, chỉ auto update

### Bug thật nằm ở đâu

`RoadmapItemData.progress` là **số được lưu**, do người dùng kéo slider trong Properties — và
không có chỗ nào tính lại nó. Trong khi đó `SubtaskSection` ngay phía trên lại **tự tính sống**
từ các issue link vào item. Nên một màn hình nói hai điều khác nhau: RM-33 ghi `Progress 100%`
còn thanh SUB-TASKS ngay trên nó ghi `1 of 2 done`.

Sửa ở frontend không đủ: số cũ đó còn chảy ra card ngoài board, thanh + sublabel bên Gantt,
tooltip biểu đồ RICE, và dialog của public roadmap. Nên **tính ở server**, mọi response đều đúng.

### Luật tính (`application/roadmaps/domain/roadmap-progress.ts`)

Khớp *đúng* luật mà `SubtaskSection` đang hiển thị, để sidebar và thanh không thể nói khác nhau:

- Có work link vào item → `round(done / total * 100)`, tính cả task con cháu (`$graphLookup`, 4 cấp).
- **Bug không được tính.** Bug là việc *phát hiện ra*, không phải việc *đã lên kế hoạch*.
- Không có work nào link vào → rơi về status của item: `done` = 100%, còn lại 0%.
- Status do tay set **không thắng được** sub-task: item đánh Done mà 1/2 task xong thì vẫn 50%.

### Đóng vòng cập nhật

- `roadmapItemRollups()` thêm vào port + Mongo repo của issue (`$addToSet` để một issue vừa là
  root vừa là con của root khác không bị đếm hai lần).
- `GetRoadmapProgressUseCase` tính một lượt cho cả roadmap; `RoadmapMapper.toResponseDto` nhận
  `progress` là **tham số bắt buộc** — không endpoint nào lỡ rơi về số cũ được.
- 6 endpoint roadmap + public roadmap đều đi qua đó.
- `ReplaceRoadmapItemsUseCase` **bỏ qua** progress client gửi lên; `roadmap-item-diff` bỏ
  `progress` khỏi `TRACKED_FIELDS` (event thật nằm ở timeline của task con).
- Frontend: hook issue giờ invalidate cả `roadmap`/`roadmaps` — đổi status task xong là số % refetch ngay.
- Properties: slider → `ProgressBar` **read-only** + dòng `roadmaps.progressAuto`
  ("Counted from this item's sub-tasks — bugs excluded." / "하위 작업 기준으로 자동 계산됩니다 (버그 제외).").

### Kiểm chứng

Backend `tsc --noEmit` sạch, `jest src/application/roadmaps` → 4 suite / 26 test pass.
Frontend `tsc --noEmit` sạch. **Chưa chạy `$graphLookup` trên DB thật** — repo không có
mongodb-memory-server, và không đụng vào `.env.local` (đó là production).

### Cần biết trước khi deploy

Item **không có task nào link vào** giờ đọc 0% trừ khi status là Done. Những % set tay trước đây
trên các item đó sẽ biến mất — đúng theo yêu cầu "chỉ auto update", nhưng là thay đổi nhìn thấy được.

## Prompt: "hiện tại khi set status là done thì nó chưa qua cột done"

> [ảnh board Roadmap] hiện tại khi set status là done thì nó chưa qua cột done

### Bug thật nằm ở đâu

`status` (Idea / Planned / In progress / Done) và `phase` (cột: Now / Next / Later / Done / …) là
hai field độc lập, không có luật nào nối chúng lại. Set Done trong panel chỉ ghi `status`, thẻ vẫn
nằm ở Now — board trông như không nghe lời, và người dùng phải kéo thêm một lần nữa. Ngược lại, kéo
thẻ vào cột Done cũng không đổi `status`, nên board dần đầy thẻ "Done" nằm ngoài cột Done.

### Luật (`frontend/src/features/roadmaps/itemStatusPhase.ts`)

Một file duy nhất giữ luật, để Select trong panel và drag trên board không thể lệch nhau:

- `doneColumn(columns)` — tìm cột Done: ưu tiên key mặc định `done`, sau đó so theo label (cột Done
  tự tạo trong *Manage columns* có key sinh ra dạng `col-…`). Không có cột Done → không suy diễn gì.
- `phaseForStatus(status, phase, columns)` — status → cột: **Done** đưa thẻ sang cột Done;
  **Done → khác** kéo thẻ ra khỏi Done về cột đầu tiên không phải Done (Now); còn lại giữ nguyên.
- `statusForPhase(phase, status, columns)` — cột → status (gương của trên): thả **vào** Done →
  Done; kéo **ra khỏi** Done khi đang Done → In progress; các move khác không đụng status.

Chỉ **Done** được suy ra. Now / Next / Later là phán đoán *khi nào làm*, không status nào đoán hộ được.

### Đấu nối

- `RoadmapItemDetail` — Select status gọi `setStatus`, ghi cả `status` + `phase`.
- `RoadmapBoardPage.onMove` — drag ghi cả `phase` + `status`.
- Cả hai đi qua `useReplaceRoadmapItems` (optimistic), nên thẻ nhảy cột ngay; backend vẫn tự đóng
  dấu `startedAt`/`completedAt` theo status như cũ (`ReplaceRoadmapItemsUseCase`).

### Kiểm chứng

`itemStatusPhase.spec.ts` — 11 test pass (vitest). Frontend `tsc --noEmit` sạch. Chưa bấm tay trên UI.

---

## Prompt 9

> mất cái nút đổi qua đổi lại giữa gantt chart và calendar, ở calendar thì chỉ show backlog item
> thôi, không show task con của backlog item

### Điều tra "mất nút"

Nút vẫn có ở board đã đăng nhập (`RoadmapBoardPage` → `?tl=` → `TimelineModeToggle` →
`GanttChart.toolbar` / `CalendarView.toolbar`). Ba khả năng, và chỉ một cái là lỗi thật:

1. **Trang share công khai (`/public/roadmaps/:token`) thật sự không có nút.** Nó render thẳng
   `RoadmapGantt` mà không truyền `toolbar` → tab Timeline của link chia sẻ chỉ có Gantt. **Đã fix.**
2. Chưa commit gì cả — app build từ git thì không có thay đổi này.
3. Working tree đang bị một session khác sửa song song, dev server compile nhầm lúc sẽ hỏng/cũ.

### Đã làm

- **`PublicRoadmapPage.tsx`** — thêm `?tl=calendar` (cùng key với board đăng nhập, nên link share
  mở đúng cách nhìn người gửi đang xem), `TimelineModeToggle`, truyền `toolbar` cho cả hai view.
- **`RoadmapCalendarView.tsx`** — `roadmapId`/`tasksByItem`/`sprintsForItem`/`scope`/`groupBySprint`
  thành optional, thêm `onOpenItem` để trang public mở `PublicRoadmapItemDialog` của nó thay vì
  peek drawer (public không có account để fetch detail).
- **`GanttChart.spec.tsx`** — 4 test giữ slot toolbar sống qua cả 3 trạng thái (có row / đang load /
  rỗng). "Nút biến mất" chính là lỗi file này tồn tại để chặn. `vite.config.ts` mở include sang `.tsx`.

### Về "calendar chỉ show backlog item"

Đã đúng từ đầu: `RoadmapCalendarView` chỉ map `chartedItems(...)` — không hề vẽ task con.
Task con vẫn xem được ở tab Calendar của board team.

### Kiểm chứng

Frontend `tsc --noEmit` sạch · `vitest run` 21 file / 156 test pass · `npm run build` sạch.

## Prompt: "khi done hiện thêm 1 cái toaster gì đó để ấn phát nhảy qua cột done"

Chuyển từ **tự nhảy cột** sang **mời, không ép**: set status xong, toast hiện lên gọi tên cột mà
status đang trỏ tới; ấn một phát thì thẻ nhảy sang đó, kệ nó thì thẻ nằm nguyên chỗ cũ.

- `RoadmapItemDetail.setStatus` — `save({ status })` trước (status luôn được ghi ngay), rồi so
  `phaseForStatus`; trùng cột hiện tại thì im lặng (Done đang nằm ở cột Done thì không mời gì cả).
- Toast: `id: roadmap-move-<itemId>` nên đổi ý hai lần liên tiếp thì toast thay nhau chứ không
  chồng hai thẻ mâu thuẫn; `duration: 10s` — đủ đọc và bấm, không dính màn hình.
- `moveToColumn` ghi qua `roadmapRef` (ref giữ query data mới nhất). Lý do: action của toast chạy
  sau vài giây, lúc đó `item`/`roadmap` của render cũ đã là ảnh chụp cũ — patch qua nó sẽ ghi đè
  luôn những gì vừa sửa ở giữa (mô tả, assignee…).
- i18n `roadmaps.moveColumnPrompt` / `roadmaps.moveColumnAction` (en + ko), có `{status}` và `{col}`.
- Drag trên board **giữ nguyên tự động** (thả vào Done → Done, kéo ra → In progress): kéo thả đã là
  hành động dứt khoát rồi, hỏi lại thêm một lần là thừa.

### Kiểm chứng

`tsc --noEmit` sạch; `vitest run` toàn bộ frontend: 22 file / 167 test pass. Chưa bấm tay trên UI.

---

## Prompt 10

> đưa chuột vào nó highlight lên, ấn vào nó hiện pop-up thêm thông tin, có nút navigate qua task
> details
> → à bỏ cái request ấn vào tui nói
> → mỗi task tự 1 màu khác nhau với lại hiển thị progress bằng màu đậm/nhạt tương ứng mỗi task luôn

Phần click/pop-up **đã bỏ** theo yêu cầu — click vẫn mở peek drawer như cũ (trong đó sẵn có
"Open full page").

### Hover highlight (`CalendarView.tsx`)

- State `hovered` giữ **id của event**, không phải id của thanh. Item chạy qua nhiều tuần bị cắt
  thành mỗi tuần một `<button>`; chỉ sáng khúc dưới con trỏ thì đúng cái cần nhìn (item kéo dài
  tới đâu) lại bị giấu. Giờ mọi khúc sáng cùng lúc, cả ở agenda mobile.
- Focus tính như hover → tab qua tháng cũng sáng đúng run đó.
- Sáng lên = đổ đậm thêm **chính màu của item** + viền `inset` (không đẩy layout). Không mint màu mới.

### Mỗi item một màu (`itemColor` trong `RoadmapGanttView.tsx`)

- Hash `item.id` → `TEAM_COLORS` — **đúng pattern và đúng bảng màu** `avatarColor`/`docTagColor`
  đang dùng, nên không có màu nào ngoài palette của sản phẩm, không cần field mới, không migration.
- Trước đây màu lấy từ **cột roadmap**, mà cả Gantt lẫn Calendar mặc định chỉ hiện cột "Now" →
  mọi thanh cùng một màu tím, màu không mang thông tin gì. Đó chính là ảnh sếp gửi.
- **Gantt dùng chung `itemColor`** → một item một màu ở cả hai view. `colorOf(phase)` bỏ;
  `barColor` chỉ còn cho swatch legend (giải thích *hình dạng* thanh, không phải màu item nào).

### Progress bằng đậm/nhạt

- `CalendarEvent.progress` (0–100) — cùng ngữ nghĩa `GanttBar.progress`: track nhạt + phần đã xong
  đổ đậm hơn, **cùng một màu**. Bảng `TINT` giữ 4 mức (track/fill × rest/hover) ở một chỗ vì đó là
  một quan hệ (fill phải đậm rõ hơn track, mà chữ vẫn phải đọc được trên cả hai).
- `segmentFill(pct, offset, span, total)` — fill tính theo **cả run**, không theo lát tuần: item 3
  tuần xong 40% thì đầy tuần 1 + một phần tuần 2, chứ không phải 40% mỗi tuần. `Segment` thêm
  `offset`/`total` để làm được việc này.
- Agenda mobile: cùng cách đọc, gấp lại thành strip 1px dưới tiêu đề.

### Kiểm chứng

`CalendarView.spec.ts` — 6 test khoá phép tính fill (bug "reset 0% mỗi thứ Hai" trông rất thuyết
phục cạnh mấy thanh 1 tuần). `tsc --noEmit` sạch · `vitest run` 23 file / 173 test pass ·
`npm run build` sạch.
